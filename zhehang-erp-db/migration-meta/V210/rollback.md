# V210 回滚说明

- V210 只新增治理表和普通组合索引，不改历史业务行。
- 应用前必须取得数据库全量备份路径并写入 `schema_migration_history.backup_reference`。
- 如需回滚，先停用 V210 代码，再逐个 `DROP INDEX` 删除本文件新增索引。
- 四张治理表默认保留审计证据；只有确认未执行任何 V210 数据修复后才能删除。
- 不允许通过重跑旧迁移替代回滚。
