# V235 系统设置规则/字段治理底座 回滚说明

- V235 上线顺序：全库备份 → 仅执行本文件 → 结构/种子核对 → 发布应用。
- 迁移前明确为`0/0`（sys_dict_type/sys_dict_data 两表均不存在）才走 A 路径新建；任意非零退出必须中止应用发布。
- 回滚：仅当上线前确认走 A 路径且尚无真实配置写入时，才可按指纹删除 3类19项种子并 DROP 两张新表。
- 走 B 路径（补齐缺失种子）或已经有真实写入时，只回滚代码/入口，绝不 DROP 或删除配置数据。
- 禁止删除表或配置数据；不得运行全量迁移脚本（db-apply-migrations.sh）代替精准回滚。
