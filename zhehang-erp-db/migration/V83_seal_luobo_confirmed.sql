-- =============================================================================
-- V83  刻章提单加"萝卜章已确认"标记(私下刻制风险提示已确认)。
-- =============================================================================
ALTER TABLE `biz_seal_order`
  ADD COLUMN `luobo_confirmed` TINYINT DEFAULT 0 COMMENT '萝卜章已确认(私下刻制):1=是' AFTER `status`;
