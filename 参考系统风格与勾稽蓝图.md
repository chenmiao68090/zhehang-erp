# 参考系统(诚路会计)风格 + 勾稽关系 学习蓝图

> 来源:东方雅达「诚路会计」代账/财税机构 ERP 截图 ~50 张。
> 目的:提炼可复用的 **UI 风格规范** 与 **业务勾稽关系模型**,指导本系统的改造与丰富。

## 一、风格规范(全系统统一应用)

1. **整体**:浅色运营后台。左侧绿色竖向主导航(可展开二级菜单)+ 顶部操作区。内容区白底卡片,主色绿(按钮/选中态),信息蓝、警示红黄。
2. **页面骨架**(几乎每个业务页都一致):
   `顶部 Tab 切换` → `筛选条(下拉/日期/关键词 + 导出按钮)` → `表格` → `底部合计行 / 右侧合计文案` → `分页`。
3. **多维 Tab**:同一数据按维度切 Tab,如 `合同收款(人员)/合同收款(部门)/合同签订(人员)/合同签订(部门)`;售后按 `回款率(人员)/回款率(部门)/续签率(人员)/续签率(部门)`。
4. **分组表头**:列头分组跨列,如 `新签 | 续签(有增值) | 续签(无增值)`,每组下挂 `数量/金额/支出/退费/核定成本/业绩` 同一组子列。
5. **合计**:表格底部固定「合计」行;或筛选条右侧「合计数量 / 合计合同金额 / 合计收款金额」实时汇总。
6. **设置区**:二级左导航(配置大类)+ 三级页内左导航(具体项)+ 顶部 Tab。配置高度数据化(标签/分类/规则/流程/审核流/称谓全部可配)。
7. **图表语言**:状态占比用环形/饼图(派单状态、开票类型、收支占比、资金账户占比);转化漏斗用倒三角(线索阶段、商机阶段);趋势用折线(历年合同收入、客户数增减)。
8. **角色驱动**:权限按模块勾选,内置「公司领导/财务主管/销售总监/销售/代账主管/代账会计/外勤主管/外勤/售后主管/售后」十类角色模板。

## 二、勾稽关系模型(系统的灵魂)

### 主链路(营销 → 履约 → 财务 → 复盘)
```
线索(来源/成本) → 商机(阶段/预计成交额) → 合同(新签/续签) → 收款(打款/核销)
      → 派单(工商/代账/税控工单) → 服务交付 → 业绩计提 → 回款率/续签率复盘
```

### 关键计算勾稽(必须在后端算,前端只展示)
- **业绩金额** = 合同金额 − 支出 − 退费 − **核定成本**
  (核定成本来自「服务项目」配置的成本,是业绩计提的扣减项)
- **业绩按 (新签 / 续签有增值 / 续签无增值)** × (数量/金额/支出/退费/核定成本) 二维拆分
- **回款率** = 实际收款 / 应收(合同)金额,按月、按人员/部门
- **续签率** = 续签数 / 到期应续数,按月、按人员/部门
- **代账户收入勾稽**:客户(代账户) → 记账/报税/社保/公积金工单 → 月度收入 → 预计续费收入
- **财务收支勾稽**:利润 = 收入(主营业务+营业外+资金调拨转入) − 支出(成本+费用+税金+资金调拨转出);各资金账户(银行卡/对公/支付宝/微信)收支占比
- **派单状态勾稽**:工单状态(未派单/正常进行/进行中预警/进行中延期/按时完成/延期完成/中止)占比,从工单流程节点的预计耗时/预警时长推导

### 维度统一(所有统计共用)
- 人员维度 / 部门维度(每个业绩、回款、续签报表都成对出现)
- 时间维度(本周/本月/本年 + 自定义起止)
- 业务类型维度(新签/续签、工商/代账/税控、企业类型:小规模/一般纳税人/零申报/个体/港澳)
- 来源维度(电话/渠道/转介绍/扫楼/抖音/百度/精准客源…,带线索成本→投入产出比)

## 三、差距分析(参考系统 vs 本系统)

本系统已有模块:order/contract/crm/hrm/finance/project/sales/channel/multidim/workflow/task/report/dashboard/system 等,骨架齐全。**主要差距在「深度」与「勾稽」**:

| 参考能力 | 本系统现状 | 改造方向 |
|---|---|---|
| 业绩管理(多维分组表+合计+计提) | 有提成结算雏形 | 重做成「销售/代账/外勤/渠道/开票」多维业绩,接真实合同+收款算业绩 |
| 统计分析(简报/画像/漏斗/趋势) | dashboard/report 较浅 | 按「营销/合同/派单/代账/财务」专题重构,接真实勾稽 |
| 设置数据化(规则/流程/审核流) | system 配置较少 | 补公海规则/线索规则/释放规则/审核流/服务项目成本 |
| 代账垂直(记账/报税/社保/公积金工单) | 有 workflow/task | 补代账工单配置 + 代账户财税申报统计 |
| 风格一致性 | 深浅混杂、多 mock | 统一浅色运营后台骨架 + 分组表头 + 合计行 |

## 四、改造路线(分期,按价值×可行性)

**P1 — 风格地基(可复用组件)**
- 抽一个 `运营后台页面骨架`:Tab + 筛选条 + 导出 + 分组表头表格 + 合计行 + 分页。
- 抽 `分组表头表格` 与 `合计行` 组件,后续所有业绩/统计页复用。

**P2 — 业绩管理(最具勾稽价值,接真实数据)**
- 后端:`业绩统计`接口,按人员/部门 × 新签/续签(有增值/无增值)聚合 合同金额/支出/退费/核定成本 → 业绩。数据源 = 真实合同(crm_contract/biz_contract)+ 收款(biz_receipt)+ 服务项目核定成本。
- 前端:销售业绩页用 P1 组件呈现(对齐参考的四 Tab + 分组表头 + 合计)。

**P3 — 统计分析专题**
- 营销简报(线索/商机/合同/回款关键指标)、来源画像(投入产出比)、线索/商机漏斗、合同趋势、财务收支占比。全部接真实勾稽。

**P4 — 设置数据化 + 代账垂直**
- 补审核流、服务项目核定成本、公海/线索/释放规则;代账工单(记账/报税/社保/公积金)配置与统计。

## 五、第一步(我接下来做)

从 **P1 风格地基** 起步——先做可复用的「分组表头 + 合计行」表格组件和页面骨架,因为:
1. 它是后面所有业绩/统计页的基础,一次投入全局受益;
2. 纯前端 + IDE 类型可校验,风险低、不依赖运行;
3. 立刻能把现有页面的视觉/结构向参考系统对齐。

随后用它把「销售业绩」页按参考样式重做,并接 P2 的真实业绩勾稽。
