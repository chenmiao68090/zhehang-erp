# 并行开发盘点报告

> 日期: 2026-06-04  
> 背景: Code 正在跑局部开发任务,本次 Codex 只做读码盘点和文档修正,不改业务代码。

## 一、当前结论

现在最适合 Codex 先做的是三件事:

1. 固定模块边界,避免 Code 后续做出重复页面。
2. 梳理 Mock/真实接口的替换顺序,避免一口气去 Mock 造成页面崩。
3. 准备验收清单,等 Code 跑完后快速审查页面、路由、接口和勾稽关系。

本次已扫描:

- 前端 `src/views`: 约 66,740 行 Vue 页面。
- 前端 `src/api`: 约 8,263 行 API/Mock 层。
- 前端静态路由: 已存在旧路径隐藏重定向。
- 后端 modules: 已有 `crm/company/channel/order/receipt/task/contract/finance` 等业务模块雏形。

## 二、路由与模块边界观察

### 已经做对的方向

- 旧 CRM 合同入口已重定向到 `/order/contract`。
- 旧销售订单入口已重定向到 `/order/bill`。
- 旧财务凭证入口已重定向到 `/finance/journal`。
- 旧组织管理入口已重定向到 `/hrm/...`。
- 线索中的客户资产页已迁到 `/customer/...`。
- 网销投产比已经从渠道管理口径迁到 `/leads/online-roi`。

这些方向建议继续保留: 旧路径可以兼容,但不要再显示在侧边栏。

### 需要后续统一的边界

| 问题 | 建议归属 |
|---|---|
| `crm/*` 旧页面仍在文件夹中 | 先保留兼容,等真实菜单确认后再删除或封存 |
| `sales/order` 与 `order/bill` | 订单只认 `/order/bill`,销售订单页不再作为主入口 |
| `finance/performance`、`task-center/commission`、`sales/performance` | 销售业绩看排行,提成结算管规则审批,财务绩效管发放入账 |
| `finance/expense`、`finance/reimburse`、`finance/cost` | 业务支出、员工报销、管理成本三类必须分账 |
| `order/finance-check` 与 `finance/journal` | 财务核对管订单收款核销,日记账管最终公司流水 |
| `collaboration/notification` 与 `system/notification` | 通知只保留系统通知主入口,协作通知做隐藏重定向 |

## 三、Mock 风险分层

### 可以继续演示,但后续要替换

- 登录 Mock 回退。
- 首页/驾驶舱 fallback 数据。
- 多维表格前端 Mock。
- 呼叫中心通话记录和波形模拟。
- 报表设计器 demo 数据。

### 属于真实业务能力缺口,不能只当作假数据

- 公海领取、回收、撞单、持有上限、冷却期。
- 企业主体库工商信息源。
- 网销线索来源、成本、渠道 ROI。
- 提单、合同、收款、财务核对、日记账之间的联动。
- 任务中心周期任务、交接、提成结算。
- 地址供应商、地址资源池、同行渠道账期和额度。

这些要按接口契约逐步后端化,不要一次性删除 localStorage 逻辑。

## 四、下一步我可以继续做的非冲突任务

### A. 等 Code 跑完后的验收

- 看 `git diff` 和改动文件范围。
- 跑前端 `npm run build`。
- 打开浏览器验证登录、首页、营销获客、客户中心、订单、财务、渠道。
- 检查控制台错误和页面白屏。
- 把结果写入 `开发交接记录.md`。

### B. 继续做只读审计

- 输出「可删除/可封存/必须保留」页面清单。
- 输出「Mock 替换接口清单」。
- 输出「菜单显示/隐藏/重定向」清单。
- 输出「财务科目归集标准」验收表。

### C. 准备下一批实现任务

优先建议:

1. 企业主体库详情页: 统一展示工商、线索、客户、合同、订单、任务、回款摘要。
2. 网销 ROI 页面: 用渠道成本、线索、成交、回款四层口径算 ROI。
3. 日记账: 建立一级模块/二级科目的固定字典和自动归集规则。
4. 同行渠道: 增加账期、授信额度、应收、地址资源占用。
5. 提成结算: 明确销售业绩、提成确认、财务发放三段链路。

## 五、协同提醒

- Code 当前跑任务时,不要同时改 `routes.ts`、全局 layout、全局样式、核心 API Mock 层。
- 如果 Code 改了这些文件,后续必须先 review 再继续扩展。
- 每次 Code 完成后,都应追加 `开发交接记录.md`,否则后续很难判断哪些改动是已验收的。
