# 开发交接记录

> 用途: Codex 与 Code 每次完成任务后都在这里记录,保证协同开发不断线。  
> 规则: 新记录写在最上方;如果没有跑验证,必须说明原因。

## 2026-06-08 Codex 私域客户行动建议支持点击

### 本次任务

持续开发第 62 轮:把私域客户详情里的“下一步行动建议”从提示文案升级为可点击动作入口。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 将行动建议步骤从静态标签改为小按钮。
- 新增 `ContactActionKey` 和 `ContactActionItem` 状态动作模型。
- 新增统一点击处理,复用工商核验、记录跟进、生成跟进任务、入库线索、生成提单、生成交付包等现有函数。
- 已建交付包客户支持从详情直接聚焦交付包并打开交付包详情。
- 提单/审批相关动作会切到对应跟进队列,减少用户自己找标签页。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。
- 浏览器回归:
  - 打开 `/leads/private-domain?tab=contacts`。
  - 点击可见首个“详情”按钮。
  - “下一步行动建议”展示可点击按钮。
  - 点击无副作用的“看交付包”,页面切到“交付包”标签并打开交付包详情。

### 下一步建议

- A: 客户详情时间线按跟进、核验、提单、交付分组展示。
- B: 客户雷达操作列按状态动态收起低优先级动作。
- C: 给高意向客户支持批量生成跟进任务。

---

## 2026-06-08 Codex 私域客户详情补下一步建议

### 本次任务

持续开发第 61 轮:在私域客户详情补“下一步行动建议”,让销售/交付打开客户后能马上判断先核验、报价、提单、盯审批还是建交付包。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 客户详情抽屉新增“下一步行动建议”面板。
- 建议面板按客户状态动态展示阶段、标题、说明和 3 个执行步骤。
- 支持待核验、今日未跟进、高意向、已报价未提单、已生成提单待审批、审批完成待交付、已建交付包等判断。
- 复用现有跟进记录、提单、交付包状态,不新增独立流程。
- 补充浅色状态面板样式和移动端单列适配。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。
- 浏览器回归:
  - 打开 `/leads/private-domain?tab=contacts`。
  - 点击可见首个“详情”按钮。
  - 详情抽屉展示“下一步行动建议”。
  - 示例客户展示“已闭环 / 看交付包 / 查逾期任务 / 安排回访”等动态建议。

### 下一步建议

- A: 把“下一步行动建议”的步骤做成可点击动作入口。
- B: 私域客户详情时间线按交付、跟进、核验分类。
- C: 客户雷达操作列按客户当前状态动态收起低优先级动作。

---

## 2026-06-08 Codex 私域客户详情补运营摘要

### 本次任务

持续开发第 60 轮:在私域客户详情统一补运营摘要,让详情页先回答今天该不该跟、最近有没有触达、是否已经转线索/交付。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 客户详情抽屉新增“运营摘要”区。
- 摘要展示今日状态、最近互动、跟进记录、转化进度四个运营判断。
- 新增客户跟进记录、队列状态、转化状态辅助函数。
- 摘要根据待核验、今日未跟进、高意向、已入库线索、已建交付包动态提示下一步动作。
- 补充抽屉摘要卡片样式和移动端单列适配。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。
- 浏览器回归:
  - 打开 `/leads/private-domain?tab=contacts`。
  - 使用开发预览账号登录后进入目标页。
  - 点击首行“详情”,详情抽屉展示“运营摘要”。
  - 抽屉展示“今日状态 / 最近互动 / 跟进记录 / 转化进度”四个摘要字段。

### 下一步建议

- A: 高意向客户详情补“生成报价/提单提醒”。
- B: 私域客户详情时间线按交付、跟进、核验分类。
- C: 联系人详情底部动作根据当前状态动态排序。

---

## 2026-06-07 Codex 批量核验增加确认和摘要

### 本次任务

持续开发第 59 轮:给私域客户批量工商核验增加安全确认,并让执行完成后的反馈包含命中/疑似重复/未命中摘要。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 批量核验前增加 `ElMessageBox.confirm` 确认弹窗。
- 确认文案提示会更新客户主体信息、重复风险和最近互动时间。
- 取消确认时直接返回,不执行核验。
- 批量核验完成后统计命中、疑似重复、未命中数量。
- 批量核验成功提示改为摘要格式。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。
- 浏览器回归:
  - 打开 `/leads/private-domain?tab=contacts`。
  - 切换到“未核验”快捷筛选。
  - 点击“批量核验 7”,出现“确认批量工商核验”弹窗。
  - 弹窗展示“更新客户主体信息、重复风险和最近互动时间”影响说明。
  - 弹窗展示“再看看 / 开始核验”按钮。
  - 点击“再看看”后弹窗关闭,没有执行核验。

### 下一步建议

- A: 给高意向队列增加批量生成跟进任务。
- B: 批量核验完成后增加结果明细抽屉。
- C: 未核验队列增加“只看新公司/只看高分客户”二级筛选。

---

## 2026-06-07 Codex 未核验客户补批量工商核验

### 本次任务

持续开发第 58 轮:给私域客户雷达的未核验队列补批量工商核验动作,让筛选出来的客户可以直接处理。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 新增 `batchVerifyingContacts` 批量核验状态。
- 新增 `batchVerifiableContacts` 计算当前列表中可核验客户。
- 客户快捷筛选条增加“批量核验 N”按钮。
- 批量核验复用现有 `privateDomainApi.verifyContact`。
- 批量核验会同步行 loading,最后统一刷新客户列表和看板统计。
- 批量核验只弹一次总结果,避免单条核验提示刷屏。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。
- 浏览器回归:
  - 打开 `/leads/private-domain?tab=contacts`。
  - 切换到“未核验”快捷筛选。
  - 页面展示未核验提示文案。
  - 页面展示“批量核验 7”按钮。
  - 未点击批量核验按钮,避免修改当前本地演示数据。

### 下一步建议

- A: 批量核验前增加确认弹窗,提示会更新客户主体信息和最近互动时间。
- B: 批量核验完成后展示命中/未命中/疑似重复数量摘要。
- C: 给高意向队列增加批量生成跟进任务。

---

## 2026-06-07 Codex 私域客户雷达补快捷筛选

### 本次任务

持续开发第 57 轮:给私域客户雷达增加日常运营快捷筛选,让电销/网销/私域人员能快速找到今天该处理的客户。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 客户雷达搜索栏下方新增快捷筛选条。
- 快捷筛选包含“全部 / 今日未跟进 / 高意向 / 未核验”。
- 快捷筛选展示当前查询结果下的数量。
- “今日未跟进”按 `lastTouchAt` 是否为今天判断。
- “高意向”按评分 >= 80 或阶段进入有意向/已报价/已成交判断。
- “未核验”按工商核验是否命中判断。
- 客户表格改为使用本地二次筛选结果。
- 重置筛选会同时重置快捷筛选。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。
- 浏览器回归:
  - 打开 `/leads/private-domain?tab=contacts`。
  - 页面展示“全部 / 今日未跟进 / 高意向 / 未核验”快捷筛选。
  - 点击“高意向”,页面展示高意向提示文案。
  - 表格仍有客户行展示,筛选状态正常。
  - 已切回“全部”筛选。

### 下一步建议

- A: 给“今日未跟进”增加一键批量生成外呼/企微跟进任务。
- B: 给“未核验”增加批量工商核验动作。
- C: 给“高意向”增加批量生成报价/提单提醒。

---

## 2026-06-07 Codex 私域客户列表补空状态行动区

### 本次任务

持续开发第 56 轮:改善私域客户雷达列表的空状态,让筛选无结果或尚未入库客户时都有下一步动作。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 客户雷达表格增加统一空状态。
- 有筛选条件时展示“当前筛选没有客户”和重置筛选动作。
- 无筛选条件且没有客户时展示“还没有私域客户”,引导批量导入或同步企微私域数据。
- 空状态展示私域客户、高意向、已入库线索三个指标。
- 空状态动作复用现有重置筛选、批量导入、同步私域数据链路。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。
- 浏览器回归:
  - 打开 `/leads/private-domain?tab=contacts`。
  - 输入不存在关键词 `不存在客户XYZ999` 并点击查询。
  - 页面显示“当前筛选没有客户”。
  - 页面展示“重置筛选 / 批量导入 / 同步私域数据”动作。
  - 点击“重置筛选”后恢复客户列表查询状态。

### 下一步建议

- A: 给客户列表补“手动新增客户”弹窗,用于少量客户快速录入。
- B: 客户列表增加“今日未跟进 / 高意向 / 未核验”快捷筛选。
- C: 客户详情里增加私域来源和最近互动的统一运营摘要。

---

## 2026-06-07 Codex 起步清单跟进直达弹窗

### 本次任务

持续开发第 55 轮:把私域首页“今日起步清单”的“当天跟进留痕”从跳页改成直接打开跟进弹窗,减少运营人员记录动作的路径。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 起步清单点击“当天跟进留痕”时,有私域客户则直接打开“记录私域跟进”弹窗。
- 弹窗沿用现有 `openFollowDialog`,会默认选中第一个客户并带出负责人/默认跟进方式。
- 如果还没有私域客户,点击时跳到批量导入页签并提示先导入或同步客户。
- 其他起步清单步骤保持原有页签跳转。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。
- 浏览器回归:
  - 打开 `/leads/private-domain`,识别到 4 个起步清单步骤。
  - 点击第二个步骤“当天跟进留痕”,成功打开“记录私域跟进”弹窗。
  - 弹窗展示客户、负责人、跟进方式、跟进结果等字段。
  - 点击“取消”后弹窗关闭。

### 下一步建议

- A: 私域客户列表空状态补“新增客户”入口。
- B: 跟进弹窗客户下拉增加最近/高意向排序。
- C: 报价提单步骤点击时优先定位到“已报价未提单”队列。

---

## 2026-06-07 Codex 私域首页增加今日起步清单

### 本次任务

持续开发第 54 轮:让私域运营首页在 0 数据或低数据时有一条清晰的当日起步路径,避免只看指标不知道怎么启动。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 在首页指标下方新增“今日起步清单”。
- 清单按私域客户入库、当天跟进留痕、报价提单推进、成交交付建包四步生成。
- 每一步根据真实数据自动显示完成/待做。
- 顶部进度条展示已完成步骤数和完成百分比。
- 每个步骤可点击跳转到对应页签或队列。
- 增加桌面和窄屏响应式样式,避免四步在小屏挤压。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。
- 浏览器回归:
  - 打开 `/leads/private-domain`,页面显示“今日起步清单”。
  - 当前本地数据下展示 `4/4 已完成`。
  - 四个步骤“私域客户入库 / 当天跟进留痕 / 报价提单推进 / 成交交付建包”均可见。

### 下一步建议

- A: 给“当天跟进留痕”点击后直接打开记录跟进弹窗。
- B: 私域客户列表空状态补“新增客户”入口。
- C: 企业微信接入配置增加缺项检查和保存后状态卡联动。

---

## 2026-06-07 Codex 私域交付空状态补行动区

### 本次任务

持续开发第 53 轮:解决私域交付包为 0 时页面只显示“暂无交付包”、运营人员不知道下一步该做什么的问题。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 交付包空状态增加动态标题和说明。
- 空状态展示三个前置队列指标:已完成待交付、审批中提单、已报价未提单。
- 增加“去生成交付包”动作,跳转到跟进记录的“已完成待交付”队列。
- 增加“导入私域客户”和“同步私域数据”动作,让空页面有实际入口。
- 当前筛选为空但全局有交付包时,额外展示“查看全部交付包”动作。
- 修正交付统计区为 6 列,匹配当前 6 个统计指标。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。
- 浏览器回归:
  - 打开 `/leads/private-domain?tab=delivery`,页面显示“还没有可推进的交付包”。
  - 空状态展示“已完成待交付 / 审批中提单 / 已报价未提单”三个指标。
  - 空状态展示“去生成交付包 / 导入私域客户 / 同步私域数据”三个动作。
  - 浏览器工具点击“去生成交付包”时发生工具侧等待超时,未完成点击验证;代码动作是本页状态切换,构建已通过。

### 下一步建议

- A: 私域首页指标为 0 时增加“今日起步清单”,把导入、记录跟进、生成提单串起来。
- B: 已报价未提单队列为空时补导入样例或手动新增客户入口。
- C: 给接入配置增加企微字段完整度和缺项检查。

---

## 2026-06-07 Codex 交付时间线合并地址事件

### 本次任务

持续开发第 52 轮:把私域交付包详情里的时间线从“成交前跟进记录”升级为交付时间线,补上地址库存锁定和 ADR 资源绑定事件。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 交付详情时间线改为统一读取 `deliveryTimelineItems`。
- 地址交付包存在锁定记录时,时间线展示“地址库存锁定”事件。
- 已绑定 ADR 时展示“ADR 资源绑定”;未绑定时展示“ADR 资源待补绑定”。
- 时间线保留成交前跟进记录,并按时间倒序合并展示。
- 空状态从“暂无成交前跟进记录”调整为“暂无交付时间线记录”,更贴合交付详情。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 浏览器回归:
  - 打开 `/leads/private-domain?tab=delivery&deliveryFilter=address_unbound` 和 `/leads/private-domain?tab=delivery&deliveryFilter=address_unlocked`,页面可正常进入交付包页签。
  - 当前本地浏览器私域交付包数据为 0,无法点击真实交付包详情验证时间线文案。
- 前端构建:已运行 `npm run build`,通过。

### 下一步建议

- A: 补一个“去资源池复核”的二次入口,绑定后直接跳转带焦点。
- B: 交付包空数据时给运营一个一键生成演示包/从已成交记录生成交付包入口。
- C: 把私域交付的地址锁定、释放、补绑定动作全部写入接口层时间线,减少前端拼装。

---

## 2026-06-07 Codex 补绑定前增加二次确认

### 本次任务

持续开发第 51 轮:给私域地址锁定补绑定 ADR 资源增加确认弹窗,避免误选资源后影响资源池和渠道应收。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 引入 `ElMessageBox` 用于补绑定前确认。
- 补绑定前展示公司、资源号、区域、供应商、年成本和详细地址。
- 确认文案展示当前锁定库存口径。
- 确认文案提示绑定后资源池会按该 ADR 反查客户和渠道应收。
- 取消确认时直接返回,不触发补绑定 API。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。
- 浏览器回归:
  - 打开 `/leads/private-domain?tab=delivery&deliveryFilter=address_unbound` 并进入交付包详情,页面标题正常。
  - 选择 `ADR00007` 后点击 `补绑定`,出现 `确认补绑定地址资源` 弹窗。
  - 弹窗展示 `ADR00007`、`杭州钱塘商务秘书有限公司`、`成本¥2,400/年`。
  - 点击 `再看看` 后弹窗关闭,没有实际补绑定。

### 下一步建议

- A: 补绑定成功后展示“去资源池复核”入口。
- B: 补绑定成功后把动作写入交付时间线。
- C: 对低匹配度资源增加更强的黄色风险提示。

---

## 2026-06-07 Codex 补绑定候选展示供应商成本

### 本次任务

持续开发第 50 轮:让私域地址补绑定下拉候选展示供应商和年成本,避免交付人员只凭地址选择。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- ADR 候选标签增加地址供应商。
- ADR 候选标签增加年成本。
- 候选标签保留 `推荐` 前缀逻辑。
- 候选展示格式统一为 `ADR编号 · 区域 · 供应商 · 成本 · 详细地址`。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。
- 浏览器回归:
  - 打开 `/leads/private-domain?tab=delivery&deliveryFilter=address_unbound` 并进入交付包详情,页面标题正常。
  - 补绑定下拉可展开。
  - 候选项展示 `杭州钱塘商务秘书有限公司` 等供应商。
  - 候选项展示 `成本¥2,400/年` 等成本信息。

### 下一步建议

- A: 补绑定前增加确认弹窗,避免误选资源。
- B: 补绑定成功后自动提示去资源池复核。
- C: 将补绑定动作记录到交付时间线。

---

## 2026-06-07 Codex 补绑定候选按库存口径排序

### 本次任务

持续开发第 49 轮:让私域地址锁定补绑定 ADR 资源时,候选资源按当前库存口径排序,降低交付人员选错地址的概率。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 新增地址资源匹配评分:同区、同城、供应商相近分别加权。
- 补绑定候选保留原先强匹配优先逻辑。
- 没有强匹配时,按相似区域、供应商和成本排序展示全部可用资源。
- 强匹配候选下拉标签自动加 `推荐` 前缀。
- 补绑定区域新增候选提示,说明当前是强匹配推荐还是相似度排序。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。
- 浏览器回归:
  - 打开 `/leads/private-domain?tab=delivery&deliveryFilter=address_unbound` 并进入交付包详情,页面标题正常。
  - 补绑定区域出现候选提示。
  - 本地样例未找到完全同区同供应商资源时,提示 `已按相似区域、供应商和成本排序,当前可选 11 个资源`。
  - 补绑定下拉可展开并展示 ADR 候选。

### 下一步建议

- A: 候选资源标签中增加成本/供应商信息,让选择时不用只看地址。
- B: 补绑定前增加二次确认,展示将绑定的公司、交付包和资源号。
- C: 补绑定成功后自动给出“去资源池复核”入口。

---

## 2026-06-07 Codex 私域地址库存列表当前锁定优先

### 本次任务

持续开发第 48 轮:让私域地址交付包详情里的库存列表优先展示当前锁定库存,避免交付人员在多个库存卡里反复找。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 地址库存列表改为通过 `addressInventoryForDelivery` 排序。
- 当前交付包已锁定的库存永远排在第一。
- 其他库存按 `可售 > 低库存 > 停售`、可售数量、渠道价排序。
- 当前锁定库存卡片新增 `当前锁定` 标签。
- 当前锁定库存卡片增加蓝色边框和左侧强调线。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。
- 浏览器回归:
  - 打开 `/leads/private-domain?tab=delivery&deliveryFilter=address_unbound` 并进入交付包详情,页面标题正常。
  - 库存列表第一张为 `杭州上城区 · 园区地址 · 钱塘商务园`。
  - 第一张库存卡片展示 `当前锁定` 标签。

### 下一步建议

- A: 补绑定下拉候选也按当前库存口径优先排序。
- B: 当前锁定库存卡增加“查看资源池/查看供应商”双向入口。
- C: 交付详情底部增加渠道应收待确认提醒。

---

## 2026-06-07 Codex 私域交付详情补地址勾稽摘要

### 本次任务

持续开发第 47 轮:让私域地址交付包详情能直接看到库存、资源池、渠道应收和毛利口径,减少交付和渠道来回查页面。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 地址锁定详情新增 `库存来源`、`资源池状态`、`渠道应收`、`毛利测算` 四格摘要。
- 未绑定 ADR 时展示断链提示,说明资源池无法反查客户和渠道应收。
- 已绑定 ADR 时提示提单、资源号和资源池复核闭环。
- 渠道应收口径改为 `业务线 + 联系人 + 付款方式 + 收款要求`。
- 摘要样式与现有详情抽屉保持一致,用轻量边框卡片增强可扫描性。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。
- 浏览器回归:
  - 打开 `/leads/private-domain?tab=delivery&deliveryFilter=address_unbound` 并进入交付包详情,页面标题正常。
  - 详情抽屉出现 `库存来源`、`资源池状态`、`渠道应收`、`毛利测算`。
  - `资源池状态` 显示 `未绑定 ADR`。
  - `渠道应收` 显示 `同行渠道 · 王经理 · 一次性付款 · 签约后 3 日内一次性付清`。

### 下一步建议

- A: 给地址交付详情增加“补绑定后自动复核资源池”的二次动作。
- B: 将地址库存列表按当前锁定口径优先排序,把最匹配库存放在最前。
- C: 将渠道应收信息同步到渠道管理或财务结算视角。

---

## 2026-06-07 Codex 地址资源池支持私域反向定位

### 本次任务

持续开发第 46 轮:让私域交付详情跳到地址资源池后,业务人员能直接看到定位来源、筛选口径和返回交付包入口。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `zhehang-erp-ui/src/views/supply/receipt.vue`
- `开发交接记录.md`

### 完成内容

- 私域 `资源池 ADRxxxxx` 跳转地址资源池时补充 `privateLock`、`packageId` 和 `source` 参数。
- 地址资源池支持读取 `privateLock=locked/unlocked` 路由参数并自动应用筛选。
- 地址资源池在带 `resourceNo` 进入时展示定位条,说明来源、筛选口径和交付包编号。
- 定位条新增 `清除定位` 和 `返回交付包` 动作。
- 页面自动滚动优先落到定位条,目标地址资源行紧跟展示并保持高亮。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。
- 浏览器回归:
  - 打开 `/supply/receipt?resourceNo=ADR00001&privateLock=unlocked&packageId=1001&source=private-domain`,页面标题正常。
  - `未锁交付` 筛选自动选中。
  - 定位条展示 `已定位地址资源 ADR00001`、`清除定位`、`返回交付包`。
  - 目标地址资源行出现蓝色高亮。

### 下一步建议

- A: 私域补绑定完成后在成功提示旁边强化“去资源池复核”的动作。
- B: 地址资源池详情侧边栏补充“私域锁定来源/交付包/客户/账期”信息。
- C: 统一资源池、私域交付包、首页风险卡的地址状态颜色和文案。

---

## 2026-06-07 Codex 私域筛选路由自动定位

### 本次任务

持续开发第 45 轮:把首页/资源池跳转到私域交付筛选后的落点调整到业务人员真正要处理的列表区域。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 私域页标签区增加稳定定位类名 `private-domain-tabs`。
- 支持带 `tab` / `followFilter` / `deliveryFilter` / `taskFilter` / `packageId` 进入页面后自动滚到业务标签区。
- `onMounted` 先应用路由队列,再加载联系人,加载完成后再次应用路由队列,避免初始化数据覆盖路由筛选。
- 标签区增加 `scroll-margin-top`,滚动后不会被顶部栏遮挡。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。
- 浏览器回归:
  - 打开 `/leads/private-domain?tab=delivery&deliveryFilter=address_unbound`,页面标题正常。
  - 页面直接定位到 `交付包` 标签。
  - `待绑资源` 筛选处于选中态,列表展示待绑资源交付包。

### 下一步建议

- A: 地址锁定补绑定成功后提供“查看资源池并高亮资源”动作。
- B: 私域交付包详情中补充资源池库存、锁定来源和渠道账期的统一详情模板。
- C: 首页/资源池/私域三处地址断链入口统一跳转文案和状态颜色。

---

## 2026-06-07 Codex 资源池增加私域锁定筛选

### 本次任务

持续开发第 44 轮:让地址资源池自身也能查看私域交付锁定状态,并把无资源号断链入口放到渠道页。

### 修改文件

- `zhehang-erp-ui/src/views/supply/receipt.vue`
- `开发交接记录.md`

### 完成内容

- 私域地址库存联动指标新增 `待绑资源`。
- 存在待绑资源时展示 `处理断链` 按钮。
- `处理断链` 跳转到私域交付包 `deliveryFilter=address_unbound`。
- 资源池筛选栏新增 `私域锁定` 下拉。
- 支持按 `已锁交付` / `未锁交付` 过滤地址资源。
- 过滤逻辑复用 `addressLockOf(row)`,与表格反查列口径保持一致。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。
- 浏览器回归:
  - 打开 `/supply/receipt`,页面标题正常。
  - `私域地址库存联动` 中出现 `待绑资源 1`。
  - 顶部出现 `处理断链` 按钮。
  - 筛选栏出现 `私域锁定` 下拉。

### 下一步建议

- A: 资源池 `处理断链` 点击后在私域页自动滚到交付包列表。
- B: 地址锁定补绑定后自动回到资源池并高亮该资源。
- C: 将地址资源状态与私域锁定状态统一成一个详情抽屉。

---

## 2026-06-07 Codex 首页暴露地址断链风险

### 本次任务

持续开发第 43 轮:把“地址锁定未绑定资源池”的断链风险从私域详情推进到老板/首页视角,让管理层一眼能看到。

### 修改文件

- `zhehang-erp-ui/src/api/private-domain.ts`
- `zhehang-erp-ui/src/views/dashboard/home.vue`
- `开发交接记录.md`

### 完成内容

- `bossSnapshot` 新增 `unboundLockCount`。
- `buildBossRisks` 新增 `地址锁定未绑定资源` 风险。
- 风险跳转到 `/leads/private-domain?tab=delivery&deliveryFilter=address_unbound`。
- 首页地址统计卡在存在断链时显示 `待绑 N · 已锁 N`。
- 首页焦点和 AI 摘要会把地址锁定风险纳入今日重点。
- 挂靠地址业务进展增加 `待绑` 数字。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。
- 浏览器回归:
  - 打开 `/dashboard/home`,页面标题正常。
  - 首页欢迎摘要出现 `地址锁定未绑定资源`。
  - `可售地址资源` 卡显示 `待绑 1 · 已锁 45`。
  - 浏览器工具输出的 Statsig 外部上报超时为工具侧日志,非本地应用构建或页面错误。

### 下一步建议

- A: 地址资源池增加 `私域锁定/待绑资源` 筛选。
- B: 首页风险卡点击后跳转并滚到对应交付筛选区域。
- C: 给地址断链风险补一个“自动尝试匹配可用资源”的批处理动作。

---

## 2026-06-07 Codex 交付包待绑资源筛选

### 本次任务

持续开发第 42 轮:让已锁地址但缺少 `ADRxxxxx` 资源编号的交付包可以被快速发现,避免断链记录藏在详情里。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 交付包筛选新增 `待绑资源`。
- 交付包统计新增 `待绑资源` 数字。
- 新增 `needsAddressResourceBinding` 判断,仅命中地址类交付包且已有锁定但未绑定 `resourceId` 的记录。
- 路由白名单支持 `deliveryFilter=address_unbound`。
- 筛选说明文案明确提示需要补绑定资源池。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。
- 浏览器回归:
  - 打开 `/leads/private-domain?tab=delivery&deliveryFilter=address_unbound`,页面标题正常。
  - 参数保留在 URL 中,后续可直接分享/跳转到该筛选。

### 下一步建议

- A: 地址资源池增加 `私域锁定` 筛选。
- B: 私域交付包详情补绑定成功后自动跳到资源池核对。
- C: 老板首页增加地址锁定断链风险卡片。

---

## 2026-06-07 Codex 历史地址锁定补绑定入口

### 本次任务

持续开发第 41 轮:解决历史私域地址锁定记录缺少 `resourceId` 时,资源池和交付包之间无法双向追溯的问题。

### 修改文件

- `zhehang-erp-ui/src/api/private-domain.ts`
- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 新增 `privateDomainApi.bindAddressResourceForLock(lockId, resourceId)`。
- 补绑定时会把可用地址资源改成 `reserved`,写入交付包编号、客户公司和锁定时间。
- 补绑定成功后会把锁定记录写回 `resourceId`,恢复 `ADRxxxxx` 资源池跳转能力。
- 私域交付包详情中,已锁定但无资源号的历史记录会展示 `未绑定资源池` 提示。
- 增加可用地址资源下拉选择和 `补绑定` 按钮。
- 候选地址会优先按城市、区域和供应商相近度筛选,无匹配时展示全部可用地址兜底。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。
- 浏览器回归:
  - 打开 `/leads/private-domain?tab=delivery&deliveryFilter=all`,页面标题正常。
  - 未强行改写本地业务数据;补绑定入口需要在“已锁定但缺少 `resourceId`”的交付包详情中触发。

### 下一步建议

- A: 地址资源池增加 `只看私域锁定/只看未绑定锁定` 筛选。
- B: 私域交付包列表增加“未绑定资源池”状态,不用打开详情也能发现断链。
- C: 交付包详情补绑定成功后自动显示 `资源池 ADRxxxxx` 按钮并可跳转。

---

## 2026-06-07 Codex 地址资源池反查交付锁定

### 本次任务

持续开发第 40 轮:补齐地址资源池与私域交付包之间的反向勾稽关系,让渠道/交付从地址资源池也能看出资源被哪个成交交付包锁定。

### 修改文件

- `zhehang-erp-ui/src/views/supply/receipt.vue`
- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 地址资源池表格新增 `私域交付锁定` 列,并放在 `地址` 后面,首屏即可看到。
- 已绑定资源的锁定记录展示 `已锁交付`、客户公司、负责人和释放日期。
- 从资源池点击 `看交付包` 会跳到私域运营交付包页,并携带 `packageId`。
- 私域交付包页支持 `packageId` 路由定位,过滤后高亮目标交付包行。
- 保持原有从交付包跳回 `ADRxxxxx` 资源池编号的链路不变,形成双向闭环。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。
- 浏览器回归:
  - 打开 `/supply/receipt?resourceNo=ADR00015`,页面标题正常。
  - 资源池首屏已能看到 `私域交付锁定` 列。
  - 打开 `/leads/private-domain?tab=delivery&deliveryFilter=all&packageId=1`,页面可正常进入且参数保留。
  - 当前本地有 1 条早期锁定记录,但未必带 `resourceId`,所以没有强行造数据验证点击链路。

### 下一步建议

- A: 给历史无 `resourceId` 的私域地址锁定记录做补绑定入口。
- B: 交付包详情展示“补绑定资源池”的提醒,避免锁定记录和地址资源池断链。
- C: 地址资源池增加“只看私域锁定/只看未绑定锁定”的筛选。

---

## 2026-06-07 Codex 地址类交付详情前置锁定区

### 本次任务

持续开发第 39 轮:解决上一轮 `锁地址` 自动滚动验证不稳定的问题,把地址类交付包详情中的 `地址库存锁定` 区域直接前置到详情第一屏。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 将地址类交付包的 `地址库存锁定` 区域从正文中部移动到详情 `meta` 卡片顶部。
- 删除 `锁地址` 按钮触发的自动滚动参数和脚本逻辑。
- `锁地址` 和 `详情` 统一打开交付包详情,地址类交付包会自然优先看到锁定区。
- 给前置地址锁定区增加 `address-lock-priority` 间距和分隔样式。
- 保留非地址类交付包的原有详情结构。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。
- 浏览器回归:
  - 访问 `/leads/private-domain?tab=delivery&deliveryFilter=address_unlocked`。
  - 点击 `锁地址` 后交付包详情抽屉正常打开。
  - `地址库存锁定` 位于详情第一屏,检测 top=164。
  - `地址库存锁定` 排在 `交付进度` 之前。
  - 浏览器控制台应用错误为 0。

### 下一步建议

- A: 地址资源池详情里反向展示关联交付包。
- B: 给历史无 `resourceId` 的锁定记录做绑定入口。
- C: 待锁地址交付包支持一键创建补货任务并带回交付包编号。

---

## 2026-06-07 Codex 清理 Sass 弃用警告

### 本次任务

持续开发第 38 轮:清理前端构建里反复出现的 Sass `@import './feishu.scss'` 弃用警告,让后续构建输出更干净。

### 修改文件

- `zhehang-erp-ui/src/styles/index.scss`
- `开发交接记录.md`

### 完成内容

- 保留 `feishu.scss` 后置加载的覆盖顺序。
- 在样式入口顶部引入 `sass:meta`。
- 将末尾的 `@import './feishu.scss'` 改为 `@include meta.load-css('./feishu.scss')`。
- 避免直接把 `@use './feishu.scss'` 放到末尾导致 Sass 构建失败。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。
- 构建输出中原先的 Sass `@import` 弃用警告已消失。

### 下一步建议

- A: 继续清理其他构建噪声和大 chunk 拆分建议。
- B: 真实浏览器手动复核 `锁地址` 自动滚动。
- C: 地址资源池详情里反向展示关联交付包。

---

## 2026-06-07 Codex 锁地址入口尝试聚焦地址区

### 本次任务

持续开发第 37 轮:点击交付包列表的 `锁地址` 时,打开交付包详情并尝试自动定位到 `地址库存锁定` 区域。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- `锁地址` 按钮调用 `openDeliveryPackage(row, 'address')`。
- 引入 `nextTick`,等待抽屉打开后执行定位逻辑。
- 新增 `focusAddressLockSection`,在当前打开的详情抽屉 DOM 中查找 `地址库存锁定` 标题。
- 对 `.el-drawer__body` 执行滚动定位,并在 160/520/900ms 多次尝试,规避抽屉动画和内容挂载延迟。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 浏览器回归:
  - 访问 `/leads/private-domain?tab=delivery&deliveryFilter=address_unlocked`。
  - 点击 `锁地址` 后,交付包详情抽屉可打开。
  - 抽屉内能找到 `地址库存锁定` 区域。
  - 浏览器控制台应用错误为 0。
- 残留风险:
  - in-app Browser 多次读取到抽屉 `scrollTop=0`,未能确认自动滚动最终落位。
  - 代码已按抽屉内部滚动容器实现,建议下一轮优先用真实浏览器视觉复核这一点。

### 下一步建议

- A: 真实浏览器手动复核 `锁地址` 自动滚动,必要时改成把地址锁定区置顶展示。
- B: 地址资源池详情里反向展示关联交付包。
- C: 给历史无 `resourceId` 的锁定记录做绑定入口。

---

## 2026-06-07 Codex 待锁地址增加快捷入口

### 本次任务

持续开发第 36 轮:让待锁地址交付包在列表右侧直接出现 `锁地址` 操作,减少交付人员判断和点击成本。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 交付包操作列宽度从 96 调整为 126。
- 地址类且未锁资源的交付包显示 `锁地址` 快捷按钮。
- `锁地址` 和 `详情` 都打开同一个交付包详情抽屉,进入后可直接选择可售地址锁定。
- 新增 `needsAddressLock` 统一判断口径,筛选和按钮共用同一逻辑。
- 新增 `.delivery-action-cell` 防止两个文本按钮挤在一起。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 浏览器回归:
  - 访问 `/leads/private-domain?tab=delivery&deliveryFilter=address_unlocked`。
  - 待锁地址列表出现 `锁地址 详情` 操作。
  - 浏览器控制台应用错误为 0。

### 下一步建议

- A: 点击 `锁地址` 后在详情抽屉中自动滚动到地址锁定区。
- B: 地址资源池详情里反向展示关联交付包。
- C: 给历史无 `resourceId` 的锁定记录做绑定入口。

---

## 2026-06-07 Codex 交付包增加待锁地址筛选

### 本次任务

持续开发第 35 轮:把地址业务交付包里“还没锁地址资源”的包单独筛出来,让渠道和交付能优先处理卡点。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 交付包汇总卡新增 `待锁地址` 指标。
- 交付包筛选新增 `待锁地址`。
- `deliveryFilter=address_unlocked` 支持从 URL 直接进入该筛选。
- 筛选逻辑限定为地址类交付包且当前没有有效地址锁定。
- 交付汇总布局从 4 列调整为 5 列,跟进汇总仍保持 4 列。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 浏览器回归:
  - 访问 `/leads/private-domain?tab=delivery&deliveryFilter=address_unlocked`。
  - 页面出现 `待锁地址` 汇总卡和筛选按钮。
  - `待锁地址` 筛选处于选中状态。
  - 当前样例数据显示待锁地址包为 2 个。
  - 浏览器控制台应用错误为 0。

### 下一步建议

- A: 给待锁地址交付包增加“一键创建补货任务”入口。
- B: 地址资源池详情里反向展示关联交付包。
- C: 给历史无 `resourceId` 的锁定记录做绑定入口。

---

## 2026-06-07 Codex 交付包列表展示锁定地址

### 本次任务

持续开发第 34 轮:让成交交付包列表直接显示地址锁定状态和资源池编号,减少进入详情查找次数。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 交付包表格新增 `锁定地址` 列。
- 已锁地址的交付包显示资源池编号 `ADRxxxxx`,点击可跳转到地址资源池定位。
- 地址类交付包未锁定时显示 `待锁定`。
- 非地址业务显示 `不涉及`,避免误导交付人员。
- 新增地址锁定列表态的轻量样式和格式化辅助函数。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 浏览器回归:
  - 访问 `/leads/private-domain?tab=delivery`。
  - 交付包页正常加载。
  - 表头包含 `锁定地址`。
  - 浏览器控制台应用错误为 0。

### 下一步建议

- A: 地址资源池详情里反向展示关联交付包。
- B: 给历史无 `resourceId` 的锁定记录做绑定入口。
- C: 交付包增加“只看待锁地址”筛选。

---

## 2026-06-07 Codex 地址资源池定位行高亮

### 本次任务

持续开发第 33 轮:让通过资源编号跳转进入地址资源池时,目标资源行自动高亮,减少运营查找成本。

### 修改文件

- `zhehang-erp-ui/src/views/supply/receipt.vue`
- `开发交接记录.md`

### 完成内容

- 地址资源池表格增加 `row-class-name`。
- 读取 `resourceNo` query 作为目标资源编号。
- 命中目标资源时给表格行添加 `target-address-row`。
- 高亮样式使用浅蓝底和左侧蓝色定位条。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 浏览器回归:
  - 访问 `/supply/receipt?resourceNo=ADR00015`。
  - 页面仅筛出 `ADR00015`。
  - 目标行包含 `target-address-row` class。
  - 浏览器控制台应用错误为 0。

### 下一步建议

- A: 交付包列表增加锁定资源编号列。
- B: 地址资源池详情里展示关联交付包。
- C: 给历史无 `resourceId` 的锁定记录做绑定入口。

---

## 2026-06-07 Codex 资源编号跳转定位地址池

### 本次任务

持续开发第 32 轮:让交付包详情中的锁定资源编号可以跳转到地址资源池,并自动按资源编号筛选。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `zhehang-erp-ui/src/views/supply/receipt.vue`
- `开发交接记录.md`

### 完成内容

- 交付包详情的 `资源池 ADRxxxxx` 标签改为可点击按钮。
- 点击后跳转到 `/supply/receipt?resourceNo=ADRxxxxx`。
- 地址资源池支持读取 `resourceNo` query。
- 进入地址资源池后自动填充搜索框并筛选对应资源。
- 监听 `resourceNo` query 变化,后续同页跳转也能更新筛选。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 浏览器回归:
  - 访问 `/supply/receipt?resourceNo=ADR00015`。
  - 搜索框自动填入 `ADR00015`。
  - 地址资源池筛选到对应资源,页面出现 `ADR00015`。
  - 浏览器控制台应用错误为 0。

### 下一步建议

- A: 地址资源池被 query 定位时高亮目标行。
- B: 交付包列表增加锁定资源编号列,减少进详情次数。
- C: 资源池详情里反向展示关联交付包和客户。

---

## 2026-06-07 Codex 交付包显示锁定资源池编号

### 本次任务

持续开发第 31 轮:把私域地址锁定生成的资源池编号展示到交付包详情里,让运营能追踪具体锁定了哪条渠道地址资源。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 交付包详情“地址库存锁定”卡片新增 `资源池 ADRxxxxx` 标签。
- 新增 `formatAddressResourceNo` 格式化资源池编号。
- 新增 `.address-resource-chip` 样式,与现有飞书式详情抽屉保持一致。
- 历史无 `resourceId` 的锁定记录不会显示该标签。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 浏览器回归:
  - 访问 `/leads/private-domain?tab=delivery`,成交交付包视图正常。
  - 页面包含交付包和地址库存锁定相关内容。
  - 浏览器控制台应用错误为 0。

### 下一步建议

- A: 支持点击 `资源池 ADRxxxxx` 跳转到 `/supply/receipt` 并定位资源。
- B: 给历史地址锁定记录补一个手动绑定资源池入口。
- C: 交付包列表列上增加“已锁地址/资源编号”摘要。

---

## 2026-06-07 Codex 私域地址锁定反向同步资源池

### 本次任务

持续开发第 30 轮:让私域交付包锁定挂靠地址后,渠道地址资源池同步预留,避免同一地址在两个页面重复可售。

### 修改文件

- `zhehang-erp-ui/src/api/private-domain.ts`
- `开发交接记录.md`

### 完成内容

- `PrivateAddressLock` 新增可选 `resourceId`。
- 私域地址锁定时,按城市、区域、供应商关键词匹配渠道地址资源池中的可用地址。
- 匹配成功后,资源池地址同步改为 `reserved`。
- 同步写入预留客户、预留时间和备注。
- 私域地址锁释放时,若存在 `resourceId`,资源池对应地址同步释放为 `available`。
- 该联动直接操作 `biz_address_resources`,避免私域 API 与渠道 API 双向 import。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 浏览器回归:
  - 访问 `/leads/private-domain?tab=delivery`,成交交付包视图正常。
  - 访问 `/supply/receipt`,地址资源池和私域地址库存联动卡正常。
  - 浏览器控制台应用错误为 0。

### 备注

- 本轮未再新增一次锁定/释放 UI 操作测试,因为当前本地已有锁定记录来自上一轮验证;后续新锁定会带 `resourceId` 并触发反向同步。

### 下一步建议

- A: 在交付包详情里展示已同步的资源池编号。
- B: 地址资源池支持点击查看关联交付包。
- C: 对历史无 `resourceId` 的锁定记录做一次迁移或手动绑定入口。

---

## 2026-06-07 Codex 资源补充上架同步私域地址库存

### 本次任务

持续开发第 29 轮:把“资源补充单付款上架”与私域地址库存打通,让采购补货后可售库存能同步影响私域成交交付。

### 修改文件

- `zhehang-erp-ui/src/api/private-domain.ts`
- `zhehang-erp-ui/src/api/channel.ts`
- `开发交接记录.md`

### 完成内容

- 新增 `syncPrivateAddressInventoryFromStockIn`。
- 地址补货上架时按城市、区域、地址类型、供应商归并到私域地址库存。
- 同步时增加 `total` 和 `available`,保留既有 `locked`。
- 自动计算月成本和渠道价,并根据可售量重算库存状态。
- 供应商名称支持运河、钱塘、滨江、义乌/云商等关键词归并。
- 修复资源补充上架地址解析:
  - 义乌地址归入金华市。
  - 宁波/温州地址归入对应城市。
  - 明细首段为城市时,使用第二段作为区域。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 浏览器回归:
  - 访问 `/supply/purchase`。
  - 对 `PO2026060003` 执行“付款”并确认。
  - 对 `PO2026060003` 执行“上架”并确认。
  - 单据状态变为“已上架”。
  - 访问 `/supply/receipt`,地址资源池从 14 条变为 17 条,可用从 8 变为 11。
  - 私域库存联动卡 `私域可售` 从 13 变为 16。
  - 浏览器控制台应用错误为 0。

### 备注

- 本次浏览器验证会修改当前本地 mock 数据: `PO2026060003` 已被置为已上架。

### 下一步建议

- A: 给私域库存卡补充“采购来源单号/最近入库时间”。
- B: 地址资源池增加“同步到私域库存/合并库存”手动动作。
- C: 私域成交锁定地址后反向同步地址资源池预留状态。

---

## 2026-06-07 Codex 渠道管理补齐经营样例数据

### 本次任务

持续开发第 28 轮:解决渠道管理、地址资源池、资源补充单默认空数据导致页面不落地的问题。

### 修改文件

- `zhehang-erp-ui/src/api/channel.ts`
- `开发交接记录.md`

### 完成内容

- 新增一次性 mock 种子版本 `2026-06-07-supply-operational-v1`。
- `load` 逻辑调整为:非空业务数据保留,空数组会补种默认经营样例。
- 补齐 4 个渠道/地址供应商:
  - 杭州运河企服园区管理有限公司。
  - 杭州钱塘商务秘书有限公司。
  - 义乌云商园企业管理有限公司。
  - 浙江企服数智平台有限公司。
- 补齐 14 条地址资源,覆盖可用、预留、已使用、已到期状态。
- 补齐 4 条资源补充单,覆盖已上架、老板待审、已审批、已付款状态。
- 补齐 8 条网销/渠道 ROI 样例数据,保留连续亏损和高 ROI 渠道复盘口径。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 浏览器回归:
  - 访问 `/supply/vendor`,能看到 4 个供应商样例。
  - 访问 `/supply/purchase`,能看到 `PO2026060001`、`PO2026060002`、`PO2026060003`、`PO2026060004`。
  - 访问 `/supply/receipt`,地址资源池显示 14 条,统计为可用 8、已预留 2、已使用 3、已到期 1。
  - 地址资源池表格展示杭州运河、杭州钱塘、义乌云商等供应商资源。
  - 访问 `/leads/online-roi`,页面已有非空投放数据,种子逻辑未覆盖既有数据。
  - 浏览器控制台应用错误为 0。

### 下一步建议

- A: 将资源补充单上架时同步私域地址库存,形成采购到销售的库存闭环。
- B: 给地址资源池增加“从私域库存导入/合并”动作,避免两边重复录入。
- C: ROI 页后续接入私域成交金额,让网销投产比直接反映回款结果。

---

## 2026-06-07 Codex 地址资源池展示私域库存联动

### 本次任务

持续开发第 27 轮:把私域成交交付包里的地址库存状态同步展示到渠道地址资源池页,解决两套地址库存口径割裂的问题。

### 修改文件

- `zhehang-erp-ui/src/views/supply/receipt.vue`
- `开发交接记录.md`

### 完成内容

- 地址资源池新增“私域地址库存联动”卡片。
- 展示私域可售、已锁定、库存预警、锁定记录。
- 展示低库存/停售明细和状态标签。
- 新增“查看交付包”与“处理补货任务”跳转入口。
- 页面加载时并行读取地址资源池、供应商和私域运营库存。
- 跳转改为 Vue Router 对象式写法,路径和 query 分离。
- 补充响应式样式,移动端单列展示。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 浏览器回归:
  - 访问 `/supply/receipt`。
  - 新卡片展示 `私域可售 13`、`已锁定 45`、`库存预警 2`、`锁定记录 1`。
  - 风险明细展示“杭州拱墅区 · 集群地址”和“义乌稠城街道 · 园区地址”。
  - 直接访问 `/leads/private-domain?tab=delivery&deliveryFilter=all`,能激活成交交付包视图。
  - 直接访问 `/leads/private-domain?tab=tasks&taskFilter=address_stock`,能激活地址库存任务筛选。
  - 浏览器控制台应用错误为 0。

### 备注

- 浏览器自动化通道执行按钮点击时偶发 CDP 超时,页面和目标路由本身验证正常。

### 下一步建议

- A: 将 `supply/receipt` 资源池和私域地址库存真正合并为同一张业务数据源。
- B: 在采购入库时自动写入私域可售库存。
- C: 地址锁定/释放时同步供应商资源状态。

---

## 2026-06-07 Codex 首页地址资源统计读取真实库存

### 本次任务

持续开发第 26 轮:把首页“可售地址资源”和“挂靠地址”业务进展从固定展示值改为读取私域地址库存真实合计。

### 修改文件

- `zhehang-erp-ui/src/api/private-domain.ts`
- `zhehang-erp-ui/src/views/dashboard/home.vue`
- `开发交接记录.md`

### 完成内容

- `bossSnapshot` 新增 `addressInventoryStats`:
  - `totalAvailable`
  - `totalLocked`
  - `riskCount`
  - `blockedCount`
- 首页统计卡“可售地址资源”改为读取真实可售库存。
- 统计卡趋势改为显示库存预警数量和已锁数量。
- 有停售/无可售量时,统计趋势状态变为 danger。
- 首页“关键业务进展”保留并更新“挂靠地址”卡,展示可售、已锁、预警数量。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 浏览器回归:
  - 访问 `/dashboard/home`。
  - “可售地址资源”统计卡显示真实库存 `13`。
  - 趋势显示 `2 个库存预警 · 已锁 45`。
  - “挂靠地址”业务进展显示 `13 套`,并展示 `可售 13，已锁 45，预警 2`。
  - 地址库存风险卡仍正常展示。
  - 浏览器控制台应用错误为 0。

### 下一步建议

- A: 建立独立“渠道地址库存”页面,集中维护库存和价格。
- B: 把私域地址库存与供应商地址资源池做数据合并,避免两套地址库存口径。
- C: 渠道应收和地址成本接入成交交付包,形成毛利闭环。

---

## 2026-06-07 Codex 首页增加地址库存风险

### 本次任务

持续开发第 25 轮:把地址库存低于阈值风险接入老板首页,让“低库存/停售”不只停留在交付详情,而是进入经营风险预警。

### 修改文件

- `zhehang-erp-ui/src/api/private-domain.ts`
- `zhehang-erp-ui/src/views/dashboard/home.vue`
- `开发交接记录.md`

### 完成内容

- `privateDomainApi.bossSnapshot` 新增读取地址库存和私域任务。
- `buildBossRisks` 新增地址库存风险识别:
  - 可售量小于等于 2。
  - 已停售或可售量为 0。
  - 同时识别当前是否已有未完成的地址补货任务。
- 首页经营风险预警新增“地址库存低于阈值”。
- 首页欢迎摘要和焦点标签加入地址库存风险提示。
- 风险卡“去处理”直接跳转到 `/leads/private-domain?tab=tasks&taskFilter=address_stock`。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 浏览器回归:
  - 访问 `/dashboard/home`。
  - 首页出现“地址库存低于阈值”风险卡。
  - 风险描述显示 2 个地址库存低于安全线,其中 1 个停售/无可售量,并识别已有 1 个补货任务。
  - 欢迎摘要包含“地址库存低于阈值”。
  - 点击风险卡“去处理”,成功进入 `/leads/private-domain?tab=tasks&taskFilter=address_stock`。
  - 地址库存任务队列和“地址库存补货”任务可见。
  - 浏览器控制台应用错误为 0。

### 下一步建议

- A: 把渠道地址库存抽成独立页面,让供应商、库存、价格、锁定记录统一维护。
- B: 渠道地址订单成交后自动计算应收、供应商成本和毛利。
- C: 首页统计卡“可售地址资源”改为读取真实库存合计。

---

## 2026-06-07 Codex 地址库存预警可生成补货任务

### 本次任务

持续开发第 24 轮:把地址挂靠库存的“低库存/停售”状态转成渠道经理可执行任务,避免库存预警只停留在页面展示。

### 修改文件

- `zhehang-erp-ui/src/api/private-domain.ts`
- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 新增 `privateDomainApi.createAddressReplenishTask`。
- 补货任务会写入私域任务池,负责人为“渠道经理”,并带上地址库存编号、供应商、可售量、已锁量、成本、渠道价、同行报价和账期核对要求。
- 同一地址库存如已有未完成补货任务,再次点击会复用旧任务,避免重复派单。
- 地址库存卡片在低库存/停售时显示“生成补货任务”。
- 跟进任务增加 `address_stock` 队列:
  - 新增“地址库存”筛选按钮。
  - 新增筛选数量统计。
  - 新增说明文案。
- 支持 URL 参数 `?taskFilter=address_stock`,方便从其他页面跳转到地址库存任务队列。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 浏览器回归:
  - 访问 `/leads/private-domain?tab=delivery&deliveryFilter=all`。
  - 打开“地址挂靠/渠道交付包”详情。
  - 确认低库存和停售地址出现 2 个“生成补货任务”按钮。
  - 点击低库存地址的“生成补货任务”。
  - 页面自动进入“跟进任务 / 地址库存”队列。
  - 表格中出现“地址库存补货”任务,动作里包含供应商可售量、同行报价和账期核对要求。
  - 浏览器控制台应用错误为 0。

### 下一步建议

- A: 把渠道地址库存做成独立的“渠道地址库存”页面,统一维护供应商、库存、成本、价格和锁定记录。
- B: 渠道地址成交后自动生成渠道应收和供应商成本,把毛利算出来。
- C: 首页/驾驶舱增加地址库存低于阈值的经营风险卡。

---

## 2026-06-07 Codex 地址交付支持库存锁定

### 本次任务

持续开发第 23 轮:针对公司有“挂靠地址卖给同行”的业务,把私域成交交付包中的地址交付从普通任务升级为可锁定、可释放、可看库存状态的地址台账。

### 修改文件

- `zhehang-erp-ui/src/api/private-domain.ts`
- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 新增地址库存模型 `PrivateAddressInventory` 和地址锁定模型 `PrivateAddressLock`。
- 新增 4 条本地地址库存种子数据,覆盖可售、低库存、停售三类状态。
- 私域仪表数据新增 `addressInventory`、`addressLocks` 返回值。
- 新增 `privateDomainApi.lockAddressForDelivery`,支持把地址库存锁到指定交付包,并同步减少可售库存、增加已锁库存。
- 新增 `privateDomainApi.releaseAddressLock`,支持释放锁定记录,并把库存数量回滚。
- 交付包详情抽屉中,当交付包属于同行/挂靠/地址业务时,展示“地址库存锁定”模块。
- 地址库存卡片展示城市区域、地址类型、供应商、可售数、已锁数、成本、渠道价和状态。
- 交付包已有锁定地址后,所有库存按钮变为“已锁定”,避免同一交付包重复占用多个地址。
- 增加释放入口,便于交付异常或客户取消时及时释放库存。
- 补充移动端单列样式,保持详情抽屉 UI 一致。

### 验证情况

- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 浏览器回归:
  - 访问 `/leads/private-domain?tab=delivery&deliveryFilter=all`。
  - 打开“地址挂靠/渠道交付包”详情。
  - 确认“地址库存锁定”模块出现,展示 4 条库存。
  - 点击第一个“锁定到本包”,库存从可售 7/已锁 13 更新为可售 6/已锁 14,顶部出现“已锁定给”和“释放”按钮。
  - 点击“释放”,库存回滚为可售 7/已锁 13,锁定提示消失。
  - 再次锁定,最终保留交付包持有地址锁定状态。
  - 浏览器控制台新增错误日志为 0。

### 下一步建议

- A: 把地址库存从私域交付包抽到“渠道管理/供应商管理”统一维护,避免后续多个页面各自维护。
- B: 渠道客户成交时自动生成渠道应收、地址成本和毛利测算。
- C: 地址库存低于阈值时生成渠道经理补货任务。

---

## 2026-06-07 Codex 私域任务列表增加筛选和处理动作

### 本次任务

持续开发第 22 轮:接住上一轮生成的主管督办任务,把私域“跟进任务”从纯展示表变成可筛选、可完成、可恢复的执行清单。

### 修改文件

- `zhehang-erp-ui/src/api/private-domain.ts`
- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 新增 `privateDomainApi.updateTaskStatus`。
- 更新普通私域任务状态时,如果该任务属于交付包,同步更新交付包内任务和交付包状态。
- 新增 `TaskFilter`,支持:
  - 全部
  - 待处理
  - 已逾期
  - 主管督办
  - 已完成
- 新增 `taskStats`,统计任务队列数量。
- 新增 `taskFilterOptions`,筛选按钮带实时数量。
- 新增 `taskFilterHint`,解释每个任务队列的处理重点。
- 新增 `filteredTasks`,任务表格改为展示筛选结果。
- 跟进任务表格新增“操作”列:
  - 未完成任务可点“完成”。
  - 已完成任务可点“恢复”。
- 任务筛选条复用跟进/交付筛选条样式,保持 UI 一致。

### 验证情况

- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 代码检查:已运行 `git diff --check`,通过。
- 浏览器回归:
  - 访问 `/leads/private-domain?tab=tasks`。
  - 筛选按钮显示 `全部 20 / 待处理 15 / 已逾期 2 / 主管督办 1 / 已完成 3`。
  - 点击“主管督办”,表格显示 1 条“督办逾期交付”任务。
  - 点击“完成”后,该任务状态变为“已完成”,操作变为“恢复”。
  - 筛选计数更新为 `待处理 14 / 已完成 4`。
  - 浏览器控制台新增错误日志为 0。

### 下一步建议

- A: 督办任务完成后,引导用户回到逾期交付任务做“恢复待处理/完成”。
- B: 地址挂靠业务补地址库存锁定/释放台账。
- C: 任务列表增加负责人/日期筛选,用于主管晨会排班。

---

## 2026-06-07 Codex 逾期交付任务可生成主管督办

### 本次任务

持续开发第 21 轮:让交付逾期风险不只停留在预警,而是能从交付包详情直接生成主管督办任务,落实到具体负责人和跟进清单。

### 修改文件

- `zhehang-erp-ui/src/api/private-domain.ts`
- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 新增 `privateDomainApi.createDeliverySupervisorTask`。
- 仅允许 `overdue` 任务生成督办。
- 督办任务写入私域任务列表:
  - 标题: `公司名称 - 督办逾期交付`。
  - 负责人: `交付主管`。
  - 优先级: `高`。
  - 截止时间: 当天 18:00。
  - 动作内容带 `来源交付任务 #任务ID`、逾期任务名称、原责任人和补救要求。
- 增加重复创建保护:同一公司、同一来源交付任务已有未完成督办时复用原任务。
- 交付包详情逾期任务新增“生成督办”按钮。
- 前端新增 `supervisorTaskCreatingIds`、`isCreatingSupervisorTask` 和 `createSupervisorTask`。

### 验证情况

- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 代码检查:已运行 `git diff --check`,通过。
- 浏览器回归:
  - 访问 `/leads/private-domain?tab=delivery&deliveryFilter=overdue`。
  - 逾期队列只显示 1 个交付包。
  - 打开详情,逾期任务展示“恢复待处理”和“生成督办”。
  - 点击“生成督办”后,提示 `已生成督办任务: 义乌市诚达财税服务部 - 督办逾期交付`。
  - 切到“跟进任务”,能看到“督办逾期交付”、负责人“交付主管”和“来源交付任务”动作内容。
  - 浏览器控制台新增错误日志为 0。

### 下一步建议

- A: 地址挂靠业务补地址库存锁定/释放台账。
- B: 首页风险卡增加交付风险优先级排序。
- C: 督办任务完成后自动把对应逾期任务恢复或关闭。

---

## 2026-06-07 Codex 首页增加私域交付逾期风险

### 本次任务

持续开发第 20 轮:把交付包详情里标记出来的逾期任务推到首页老板风险卡,让管理层不用进入私域页面也能看到交付异常。

### 修改文件

- `zhehang-erp-ui/src/api/private-domain.ts`
- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- `buildBossRisks` 新增“私域交付逾期未处理”风险。
- 风险判断规则:
  - 交付包未完成。
  - 包内存在 `overdue` 任务。
- 风险等级:
  - 逾期任务数 3 个及以上为“高”。
  - 1-2 个为“中”。
- 风险文案显示逾期交付包数量和逾期任务数量。
- 风险入口直达 `/leads/private-domain?tab=delivery&deliveryFilter=overdue`。
- 私域页面新增 `routeDeliveryFilters`,支持从 URL 自动选中交付包页和指定交付筛选队列。

### 验证情况

- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 代码检查:已运行 `git diff --check`,通过。
- 浏览器回归:
  - 访问 `/dashboard/home`。
  - 首页“经营风险预警”出现“私域交付逾期未处理”。
  - 文案显示 `1 个交付包存在 1 个逾期任务...`。
  - 点击该风险“去处理”,跳转到 `/leads/private-domain?tab=delivery&deliveryFilter=overdue`。
  - 私域页面自动选中“交付包”页签和“有逾期 1”筛选。
  - 表格只显示 1 个逾期交付包。
  - 浏览器控制台新增错误日志为 0。

### 下一步建议

- A: 逾期交付包详情里增加“生成主管督办任务”动作。
- B: 首页风险卡增加“交付未启动”和“交付逾期”的优先级排序策略。
- C: 地址挂靠业务补地址库存锁定/释放台账。

---

## 2026-06-07 Codex 交付任务支持标记逾期

### 本次任务

持续开发第 19 轮:补齐“有逾期”筛选的数据入口,让交付主管能在交付包详情里把真实异常任务标记出来,并能恢复为待处理。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 交付包详情任务行新增动作:
  - 待处理任务显示“标记逾期”。
  - 已逾期任务显示“恢复待处理”。
  - 已完成任务不显示逾期动作,避免误改。
- 抽出 `updateDeliveryTaskState`,统一处理完成、逾期、恢复待处理三类状态更新。
- `toggleDeliveryTask` 改为复用统一状态更新函数。
- 新增 `markDeliveryTaskOverdue`。
- 新增 `restoreDeliveryTask`。
- 标记逾期后继续复用原有交付包状态勾稽,逾期任务会让交付包保持“进行中”。

### 验证情况

- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 代码检查:已运行 `git diff --check`,通过。
- 浏览器回归:
  - 访问 `/leads/private-domain?tab=delivery`。
  - 打开首个交付包详情。
  - 待处理任务显示 3 个“标记逾期”按钮。
  - 点击 1 个“标记逾期”后,详情里显示 `1` 个已逾期,交付风险变为“有 1 个任务逾期,需要主管介入”。
  - 逾期任务显示“恢复待处理”。
  - 筛选按钮从 `有逾期 0` 变为 `有逾期 1`。
  - 关闭抽屉后点击“有逾期”,表格只显示 1 个交付包。
  - 浏览器控制台新增错误日志为 0。

### 下一步建议

- A: 首页增加“交付进行中但逾期任务未处理”风险卡。
- B: 逾期交付包详情里增加“生成主管督办任务”动作。
- C: 地址挂靠业务补地址库存锁定/释放台账。

---

## 2026-06-07 Codex 私域交付包增加状态筛选

### 本次任务

持续开发第 18 轮:交付包有详情后,继续补列表管理能力,让交付主管能按待启动、进行中、逾期、已完成快速处理交付包,避免交付包变多后只能扫长表。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 新增 `DeliveryFilter`,支持 `all / not_started / in_progress / overdue / done`。
- 新增 `deliveryFilter`,默认显示全部交付包。
- 新增 `deliveryStats`,统一计算交付包数、任务数、待推进包、逾期包。
- 新增 `deliveryFilterOptions`,筛选按钮带实时数量:
  - 全部
  - 待启动
  - 进行中
  - 有逾期
  - 已完成
- 新增 `deliveryFilterHint`,说明每个队列的处理重点。
- 新增 `filteredDeliveryPackages`,交付表格改为展示筛选后的数据。
- 筛选条复用跟进记录筛选条样式,并补齐窄屏上下排列。

### 验证情况

- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 代码检查:已运行 `git diff --check`,通过。
- 浏览器回归:
  - 访问 `/leads/private-domain?tab=delivery`。
  - 筛选按钮显示 `全部 3 / 待启动 2 / 进行中 1 / 有逾期 0 / 已完成 0`。
  - 点击“待启动”,表格显示 2 条交付包,进度均为 `0%`。
  - 点击“进行中”,表格显示 1 条交付包,进度为 `40%`。
  - 点击“有逾期”,表格显示“当前筛选下暂无交付包”。
  - 浏览器控制台新增错误日志为 0。

### 下一步建议

- A: 首页增加“交付进行中但逾期任务未处理”风险卡。
- B: 给任务增加“标记逾期/恢复待处理”动作,用于模拟和真实交付异常处理。
- C: 建地址库存台账,把挂靠地址的锁定、释放、供应商、渠道客户串起来。

---

## 2026-06-07 Codex 私域交付包增加详情抽屉

### 本次任务

持续开发第 17 轮:补齐私域交付包详情页,把订单、回款、任务、进度、跟进记录集中在统一的飞书化详情抽屉里,减少交付主管在列表和其他页面之间来回找信息。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 交付包表格新增“详情”操作入口。
- 新增 `deliveryDrawer`,复用 `BusinessDetailDrawer` 统一详情页模板。
- 详情抽屉展示:
  - 业务线、负责人、来源提单、订单状态、金额、付款方式。
  - 服务项目、收款要求、创建时间、最晚节点。
  - 交付进度、已完成/待处理/逾期/总任务数。
  - 交付核对:成交来源、交付风险、回款核对、客户入口。
  - 完整任务清单,支持在抽屉内直接勾选任务。
  - 最近跟进记录,用于回看成交前沟通内容。
- 新增“查看客户”和“查看提单”动作,让交付详情能反向追溯客户和提单。
- 抽屉内勾选任务后同步刷新列表进度和抽屉进度。
- 补齐窄屏响应式样式,避免详情抽屉里的统计格挤压。

### 验证情况

- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 代码检查:已运行 `git diff --check`,通过。
- 浏览器回归:
  - 访问 `/leads/private-domain?tab=delivery`。
  - 交付包表格出现 3 个“详情”按钮。
  - 点击首个详情,抽屉展示 `PRIVATE DELIVERY`、交付进度、交付核对、任务清单、查看客户、查看提单。
  - 抽屉内显示 5 条交付任务和成交前电话跟进记录。
  - 在抽屉内勾选 1 个待处理任务后,抽屉进度更新到 `40%`,已完成任务数变为 2,列表首行进度也同步为 `40%`。
  - 重新加载干净标签后再次打开详情,没有新增浏览器错误日志。

### 下一步建议

- A: 首页增加“交付进行中但逾期任务未处理”风险卡。
- B: 交付包详情增加“回访/续费任务一键生成”。
- C: 渠道地址业务补“地址库存锁定/释放”台账,避免挂靠地址卖超。

---

## 2026-06-07 Codex 交付任务可直接勾选完成

### 本次任务

持续开发第 16 轮:把私域交付包里的“任务拆解”从静态标签改成可操作任务,交付主管可在交付包页直接勾选完成,并实时刷新交付包状态和进度。

### 修改文件

- `zhehang-erp-ui/src/api/private-domain.ts`
- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 新增 `privateDomainApi.updateDeliveryTaskStatus`,支持按交付包和任务 ID 更新交付任务状态。
- 交付任务状态同步写回全局私域任务列表,避免交付包页和跟进任务页状态不一致。
- 交付包状态自动勾稽:
  - 全部任务完成 -> `done`。
  - 任一任务完成或逾期 -> `in_progress`。
  - 全部待处理 -> `created`。
- 交付包“任务拆解”列改为可勾选胶囊任务,完成/逾期使用不同颜色标识。
- 勾选后局部刷新交付包数据,并重新加载首页经营快照,确保风险预警同步。

### 验证情况

- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 代码检查:已运行 `git diff --check`,通过。
- 浏览器回归:
  - 访问 `/leads/private-domain?tab=delivery`。
  - 确认“交付包”页签下存在 12 个可勾选交付任务。
  - 勾选首个交付任务后,首个交付包进度从 `0%` 变为 `20%`。
  - 页面出现“进行中”,完成任务胶囊数量变为 1。
  - 切换到“跟进任务”页签,可看到任务状态同步后的“已完成”状态。
  - 浏览器控制台错误日志为 0。

### 下一步建议

- A: 交付包页增加“批量完成本阶段/一键生成回访任务”,降低交付主管重复点击。
- B: 首页增加“交付包进行中但逾期任务未处理”风险。
- C: 私域交付包增加简易详情抽屉,统一展示订单、任务、跟进、回款和交付备注。

---

## 2026-06-07 Codex 私域交付包增加进度列

### 本次任务

持续开发第 15 轮:在私域交付包列表增加交付进度,让老板和交付主管不用逐条数任务,即可判断交付是否启动、推进到多少。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 交付包表格新增“交付进度”列。
- 新增 `deliveryProgress`,按交付包任务完成数计算百分比。
- 新增 `deliveryProgressStatus`,根据完成、逾期、未启动状态设置进度条样式。
- 进度条右侧显示明确百分比文本,避免 Element Plus 默认进度文本在表格里不够明显。
- 新增 `.delivery-progress-cell` 样式,让进度条和百分比在表格内稳定对齐。

### 验证情况

- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 代码检查:已运行 `git diff --check`,通过。
- 浏览器回归:
  - 访问 `/leads/private-domain?tab=delivery`。
  - 确认“交付包”表格出现“交付进度”列。
  - 当前三条交付包均显示 `0%`。
  - DOM 中 `.delivery-progress-cell` 数量为 3,与交付包数量一致。
  - 浏览器控制台错误日志为 0。

### 下一步建议

- A: 交付任务可在交付包页直接勾选完成,并实时刷新进度。
- B: 进度大于 0 自动把交付包状态改为“进行中”。
- C: 进度 100% 自动把交付包状态改为“已完成”,并同步首页风险。

---

## 2026-06-07 Codex 首页增加私域交付包未启动风险

### 本次任务

持续开发第 14 轮:让老板首页能看到“已建交付包但任务未推进”的交付断档风险,并可直接跳转到私域交付包页处理。

### 修改文件

- `zhehang-erp-ui/src/api/private-domain.ts`
- `开发交接记录.md`

### 完成内容

- `buildBossRisks` 新增“私域交付包未启动”风险。
- 风险判断规则:
  - 交付包状态为 `created`。
  - 交付包内已有任务。
  - 任务全部仍为 `pending`。
- 风险等级:
  - 3 个及以上为“高”。
  - 1-2 个为“中”。
- 风险入口直达 `/leads/private-domain?tab=delivery`,方便从首页进入交付包页。

### 验证情况

- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 代码检查:已运行 `git diff --check`,通过。
- 浏览器回归:
  - 访问 `/dashboard/home`。
  - 首页风险卡出现“私域交付包未启动 / 高”。
  - 文案显示 `3 个私域交付包已创建但首批任务仍未推进...`。
  - 点击该风险“去处理”,跳转到 `/leads/private-domain?tab=delivery`。
  - 页面自动选中“交付包”页签并展示“成交交付包”内容。
  - 浏览器控制台错误日志为 0。

### 下一步建议

- A: 交付包页增加“交付进度”列,按任务完成数展示百分比。
- B: 任务完成后同步更新交付包状态为进行中/已完成。
- C: 首页增加“交付包进行中但逾期任务未处理”风险。

---

## 2026-06-07 Codex 交付包创建后自动关闭交付待办

### 本次任务

持续开发第 13 轮:补齐“订单完成自动生成交付待办”后的反向闭环,当销售/交付创建交付包后,自动把原“生成成交交付包”待办标记为已完成。

### 修改文件

- `zhehang-erp-ui/src/api/private-domain.ts`
- `开发交接记录.md`

### 完成内容

- 新增 `closeDeliveryHandoffTasks` 辅助函数。
- 创建私域交付包时,自动匹配同公司、同提单号、标题/动作包含“生成交付包”的待办。
- 匹配到待办后:
  - 状态从 `pending` 改为 `done`。
  - 动作内容追加“已生成交付包 + 时间”。
- 新增交付拆解任务时,保留已关闭的历史待办,用于回溯谁在什么时候完成交接。

### 验证情况

- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 代码检查:已运行 `git diff --check`,通过。
- 浏览器回归:
  - 先确认 `TD202606071554` 已完成审批并存在“义乌市诚达财税服务部 - 生成成交交付包”待办。
  - 在私域“客户雷达”点击义乌客户“交付包”。
  - 回到“跟进任务”,确认原待办状态变为“已完成”。
  - 任务动作追加“已生成交付包 2026-06-07 10:00”。
  - 新增的交付拆解任务仍正常出现在任务列表。
  - 浏览器控制台错误日志为 0。

### 下一步建议

- A: 首页增加“已完成未建包/已建包未完成首个任务”两类交付风险。
- B: 交付包详情补进度条,按任务完成数计算交付进度。
- C: 对挂靠地址交付包增加地址库存锁定状态,避免卖超。

---

## 2026-06-07 Codex 订单完成自动生成私域交付待办

### 本次任务

持续开发第 12 轮:订单/提单审批完成后,自动在私域跟进任务里生成“生成成交交付包”待办,避免销售成交后忘记交付交接。

### 修改文件

- `zhehang-erp-ui/src/api/order.ts`
- `开发交接记录.md`

### 完成内容

- 订单模块新增私域任务/交付包 localStorage key。
- `syncPrivateOrderStatus` 在订单状态同步到私域跟进后,若订单已完成审批,自动调用交付待办生成逻辑。
- 自动待办生成规则:
  - 仅 `completed` 订单触发。
  - 如果同客户/同提单已有交付包,不重复生成。
  - 如果同客户同提单已存在“生成成交交付包”任务,不重复生成。
  - 待办标题为“客户名称 - 生成成交交付包”,动作里带提单号和回款/合同/资料核对要求。

### 验证情况

- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 代码检查:已运行 `git diff --check`,通过。
- 浏览器回归:
  - 在私域客户“义乌市诚达财税服务部”下新增报价跟进 `¥18,800`。
  - 从私域跟进记录生成提单草稿 `TD202606071554`。
  - 在提单系统真实走完“提交审批 -> 主管审批 -> 财务确认 -> 老板终审”。
  - 回到私域“跟进任务”,确认出现“义乌市诚达财税服务部 - 生成成交交付包”。
  - 任务动作包含 `TD202606071554` 和“生成交付包并核对回款/合同/资料”。
  - 浏览器控制台错误日志为 0。

### 下一步建议

- A: 在“已完成待交付”队列里把自动待办和生成交付包按钮并排展示。
- B: 完成交付包创建后自动把该“生成成交交付包”待办标记为已完成。
- C: 首页风险预警增加“订单已完成但交付包未创建”的数量和直达入口。

---

## 2026-06-07 Codex 私域交付包补订单摘要

### 本次任务

持续开发第 11 轮:在私域交付包里补齐订单金额、付款方式、审批状态和收款要求,让交付团队不用只拿提单号再去别的页面查资料。

### 修改文件

- `zhehang-erp-ui/src/api/private-domain.ts`
- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- `PrivateDeliveryPackage` 新增订单快照字段:
  - `orderStatus`
  - `orderAmount`
  - `paymentMethod`
  - `paymentTimeReq`
  - `orderItemNames`
- 创建交付包时按 `orderId/orderNo` 反查提单,把订单摘要写入交付包。
- 新增 `hydrateDeliveryPackages`,在读取仪表盘、交付包列表、客户时间线、老板快照时自动回填历史交付包。
- 交付包表格新增“订单摘要”列,展示金额、审批状态、付款方式和收款要求。
- 客户详情时间线里的交付记录追加订单金额,增强从客户详情追溯订单的能力。

### 验证情况

- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 代码检查:已运行 `git diff --check`,通过。
- 浏览器回归:
  - 访问 `/leads/private-domain?tab=delivery`。
  - 确认“交付包”表格出现“订单摘要”列。
  - 新建“浙江朗和装饰工程有限公司”交付包后,显示 `¥23,800 / 草稿 / 分期付款 / 签约后支付首款...`。
  - 刷新后历史“杭州启辰企业管理有限公司”交付包自动从 `¥0/未填` 回填为 `¥12,800 / 待主管 / 一次性付款 / 签约后 3 日内一次性付清`。
  - 浏览器控制台错误日志为 0。

### 下一步建议

- A: 提单审批完成时自动推送“生成交付包”待办到任务中心。
- B: 交付包详情补“合同/回款/发票/任务进度”四块摘要。
- C: 对超过收款要求仍未确认回款的交付包增加首页风险预警。

---

## 2026-06-07 Codex 私域交付包关联来源提单

### 本次任务

持续开发第 10 轮:把私域成交交付包和来源提单号打通,让销售提单、财务审批、交付任务之间有可追溯的勾稽关系。

### 修改文件

- `zhehang-erp-ui/src/api/private-domain.ts`
- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- `PrivateDeliveryPackage` 新增 `orderId`、`orderNo` 字段。
- 创建私域交付包时自动读取同一客户最近一条带提单号的跟进记录,写入交付包。
- 客户详情时间线里展示“来源提单:xxx”,方便从详情页回溯订单来源。
- 交付包表格新增“来源提单”列,已关联显示提单号,未关联显示“未关联”。
- 客户下一步动作里追加交付包对应提单号,减少销售和交付口头确认。

### 验证情况

- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 代码检查:已运行 `git diff --check`,通过。
- 浏览器回归:
  - 访问 `/leads/private-domain?tab=follow&followFilter=all`。
  - 从跟进记录生成提单草稿,确认页面出现 `TD202606071542`、`TD202606079835` 等提单号。
  - 切到“客户雷达”,点击“杭州启辰企业管理有限公司”行内“交付包”。
  - 确认自动进入“交付包”页签,表格展示“来源提单”列,且“财税服务交付包”显示 `TD202606071542`。
  - 浏览器控制台错误日志为 0。

### 下一步建议

- A: 交付包详情里补“订单摘要/回款节点/合同状态”,让交付团队不用跳页面查资料。
- B: 提单审批完成时自动触发交付包待办,让“已完成待交付”队列真正闭环。
- C: 把交付包任务同步到任务中心,形成销售、财税、工商多角色协作。

---

## 2026-06-07 Codex 私域跟进增加交付动作

### 本次任务

持续开发第 9 轮:在私域跟进队列里补上交付动作,让审批完成但未交付的客户可以直接从跟进记录生成交付包。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 私域“跟进记录”表格新增“交付”列。
- 根据提单/交付状态展示不同动作:
  - 已有交付包 -> 显示“已建包”。
  - 提单已完成且未建包 -> 显示“生成交付包”按钮。
  - 已提单但审批未完成 -> 显示“待审批”。
  - 未生成提单 -> 显示“待提单”。
- 新增 `hasDeliveryPackage`、`canCreateDeliveryFromFollow`、`createDeliveryFromFollow` 三个前端辅助函数。
- 点击“生成交付包”复用现有私域交付包创建能力,避免重复实现交付拆解逻辑。

### 验证情况

- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 代码检查:已运行 `git diff --check`,通过。
- 浏览器回归:
  - 访问 `/leads/private-domain?tab=follow&followFilter=all`。
  - 确认跟进记录表格新增“交付”列。
  - 确认 `TD202606071542 / 待主管` 对应交付状态显示“待审批”。
  - 确认“浙江朗和装饰工程有限公司”这条未提单报价记录显示“待提单”。
  - 浏览器控制台错误日志为 0。
- 当前本地样例没有“已完成待交付”记录,所以“生成交付包”按钮可见性通过状态逻辑覆盖,未实际点击生成。

### 下一步建议

- A: 在本地样例补一条“已完成待交付”的私域记录,便于完整演示交付按钮。
- B: 提单审批完成时自动标记私域交付动作,减少销售手动判断。
- C: 交付包生成后把订单号/提单号写入交付包,方便财务和交付追溯。

---

## 2026-06-07 Codex 风险卡片直达私域处理队列

### 本次任务

持续开发第 8 轮:让首页风险预警不只是提示问题,而是点击后直接打开私域跟进页并自动选中对应处理队列。

### 修改文件

- `zhehang-erp-ui/src/api/private-domain.ts`
- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 私域风险 `path` 增加深链参数:
  - 高意向待复联 -> `followFilter=next_touch`。
  - 已报价未提单 -> `followFilter=quote_no_order`。
  - 提单待审批 -> `followFilter=order_pending`。
  - 审批完成待交付 -> `followFilter=completed_no_delivery`。
- 私域运营页新增 URL 参数识别:
  - `tab=follow` 自动切到“跟进记录”页签。
  - `followFilter=...` 自动选中对应快捷队列。
- 监听路由变化,同页面内再次跳转也能更新筛选状态。

### 验证情况

- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 代码检查:已运行 `git diff --check`,通过。
- 浏览器回归:
  - 访问 `/dashboard/home`。
  - 点击“私域已报价未提单”风险卡片的“去处理”。
  - 确认 URL 变为 `/leads/private-domain?tab=follow&followFilter=quote_no_order`。
  - 确认页面自动进入“跟进记录”并展示“已报价未提单”队列。
  - 确认表格只显示“浙江朗和装饰工程有限公司”这条待提单记录。
  - 浏览器控制台错误日志为 0。

### 下一步建议

- A: 继续把驾驶舱风险卡片也接入同样深链。
- B: 在私域跟进页顶部显示“当前来自风险预警”的上下文提示。
- C: 审批完成后支持一键生成交付包。

---

## 2026-06-07 Codex 首页接入私域报价提单风险

### 本次任务

持续开发第 7 轮:把私域跟进快捷队列里的关键卡点接入首页“经营风险预警”,让老板不用进入私域页也能看到报价、提单、审批和交付断点。

### 修改文件

- `zhehang-erp-ui/src/api/private-domain.ts`
- `开发交接记录.md`

### 完成内容

- 私域老板风险计算新增三类销售卡点:
  - 私域已报价未提单。
  - 私域提单待审批。
  - 私域审批完成待交付。
- 风险描述直接给出数量和处理建议。
- 私域风险返回数量从 4 项调整为 6 项,首页仍统一截取前 6 项,不会挤爆右侧风险栏。
- 保留原有私域高意向待复联、撞单仲裁、沉默客户唤醒和成交未建交付包风险。

### 验证情况

- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 代码检查:已运行 `git diff --check`,通过。
- 浏览器回归:
  - 访问 `/dashboard/home`。
  - 确认“经营风险预警”出现“私域已报价未提单”和“私域提单待审批”。
  - 确认“私域高意向待复联”仍保留。
  - 确认原有“网销ROI低于目标”“渠道应收超账期”等基础风险仍保留。
  - 浏览器控制台错误日志为 0。

### 下一步建议

- A: 私域审批完成后自动生成交付包并防重复。
- B: 驾驶舱接入同样的私域报价/提单风险。
- C: 将风险卡片点击后带筛选参数打开私域跟进队列。

---

## 2026-06-07 Codex 私域跟进快捷处理队列

### 本次任务

持续开发第 6 轮:把私域跟进记录从流水账改成销售每天能处理的队列,一键筛出已报价未提单、已提单待审批、审批完成待交付和待复联客户。

### 修改文件

- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 私域“跟进记录”页签新增快捷筛选按钮组:
  - 全部。
  - 已报价未提单。
  - 已提单待审批。
  - 已完成待交付。
  - 待复联。
- 每个筛选项实时显示对应数量。
- 筛选后保留上方总览指标,下方表格只显示当前处理队列。
- 增加筛选说明文案,让销售知道当前队列要做什么动作。
- 补充筛选按钮样式,保证窄屏或按钮较多时可换行。

### 验证情况

- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 代码检查:已运行 `git diff --check`,通过。
- 浏览器回归:
  - 访问 `/leads/private-domain`,进入“跟进记录”。
  - 确认筛选按钮显示:全部 2、已报价未提单 1、已提单待审批 1、已完成待交付 0、待复联 2。
  - 点击“已报价未提单 1”,确认只显示“浙江朗和装饰工程有限公司”且仍可生成提单。
  - 点击“已提单待审批 1”,确认只显示 `TD202606071542 / 待主管`。
  - 浏览器控制台错误日志为 0。

### 下一步建议

- A: 审批完成后自动生成私域交付包并回写状态。
- B: 将“已报价未提单”和“已提单待审批”接入首页风险预警。
- C: 给跟进记录增加按负责人/来源/服务线的筛选。

---

## 2026-06-07 Codex 提单状态回写私域跟进

### 本次任务

持续开发第 5 轮:把私域生成的提单草稿与提单审批流打通,提交审批、财务确认、老板终审、驳回、取消后都能回写私域跟进记录和客户下一步动作。

### 修改文件

- `zhehang-erp-ui/src/api/order.ts`
- `zhehang-erp-ui/src/api/private-domain.ts`
- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 私域跟进记录新增 `orderStatus`,用于承接提单状态。
- 订单 API 新增私域状态同步 helper:
  - 按 `orderId` 或 `orderNo` 找到对应私域跟进记录。
  - 自动更新跟进记录的 `orderStatus` 和下一步动作。
  - 自动更新私域客户的下一步动作、最近互动时间和标签。
- 订单状态变更点接入私域回写:
  - 提交审批 -> 待主管审批。
  - 主管通过 -> 待财务确认或已完成。
  - 财务确认 -> 待老板终审或已完成。
  - 老板终审 -> 已完成。
  - 驳回 -> 已驳回。
  - 取消 -> 已取消。
- 私域时间线显示“提单号 + 提单状态”。
- 私域跟进记录表格的“提单”列显示双标签:提单号与当前审批状态。

### 验证情况

- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 代码检查:已运行 `git diff --check`,通过。
- 浏览器回归:
  - 访问 `/order/bill`,对私域生成的 `TD202606071542` 点击“提交审批”并确认。
  - 确认提单系统中该单状态变为“待主管审批”。
  - 访问 `/leads/private-domain`,进入“跟进记录”。
  - 确认同一提单显示 `TD202606071542 / 待主管`。
  - 确认下一动作自动回写为“提单 TD202606071542 已提交审批,等待主管审批”。
  - 浏览器控制台错误日志为 0。

### 下一步建议

- A: 给私域跟进记录增加快捷筛选:已报价未提单、已提单待审批、审批完成待交付。
- B: 提单完成后自动生成私域交付包,并避免重复生成。
- C: 首页风险增加“私域已提单未审批超时”提醒。

---

## 2026-06-07 Codex 私域报价生成提单草稿

### 本次任务

持续开发第 4 轮:把私域跟进记录里的报价/成交结果接入提单系统,让销售从私域报价可以一键生成提单草稿,后续再由提单系统提交审批和财务核对。

### 修改文件

- `zhehang-erp-ui/src/api/private-domain.ts`
- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 私域跟进记录新增 `orderId`、`orderNo`,用于回写已生成的提单草稿。
- 新增 `privateDomainApi.createOrderDraftFromFollowRecord(recordId)`:
  - 仅允许已报价、已成交或带报价金额的跟进记录生成提单。
  - 根据私域客户业务线/需求自动映射提单服务类型和服务周期。
  - 自动生成 `draft` 状态提单,不越过提交审批、财务核对和老板终审。
  - 自动写回跟进记录的提单号,并更新客户下一步动作为“核对后提交审批”。
- 私域客户时间线新增“提单草稿:订单号”信息。
- 私域运营页“跟进记录”表格新增“提单”列:
  - 未报价显示“待报价”。
  - 已报价/成交显示“生成提单”。
  - 已生成后显示提单号标签。

### 验证情况

- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 代码检查:已运行 `git diff --check`,通过。
- 浏览器回归:
  - 访问 `/leads/private-domain`,进入“跟进记录”。
  - 确认报价记录出现“生成提单”按钮。
  - 点击生成后出现提单号 `TD202606071542`。
  - 访问 `/order/bill`,确认同一提单号出现在提单系统,状态为“草稿”,客户为“杭州启辰企业管理有限公司”,金额为 `¥12,800`。
  - 浏览器控制台错误日志为 0。

### 下一步建议

- A: 从提单草稿继续做“提交审批后自动回写私域跟进时间线”。
- B: 私域已成交记录直接生成交付包,并与提单/回款节点做状态联动。
- C: 给跟进记录增加筛选,让销售快速看“已报价未提单”“已提单未审批”。

---

## 2026-06-07 Codex 首页私域风险预警

### 本次任务

持续开发第 3 轮:把私域运营里的关键风险自动推到首页老板视角,让老板不用进入私域页也能看到高意向待复联、撞单待仲裁、沉默客户和成交未交付等问题。

### 修改文件

- `zhehang-erp-ui/src/api/private-domain.ts`
- `zhehang-erp-ui/src/views/dashboard/home.vue`
- `开发交接记录.md`

### 完成内容

- 新增 `PrivateBossRisk` 风险项类型。
- `privateDomainApi.bossSnapshot()` 新增返回 `risks`。
- 新增私域风险计算:
  - 高意向/已报价客户仍需复联。
  - 疑似或强命中撞单客户待主管仲裁。
  - 沉默客户需要唤醒。
  - 已成交但未生成交付包的客户。
- 首页加载私域老板指标时,自动将私域风险合并到“经营风险预警”前列。
- 保留原有网销 ROI、渠道应收、地址资源和交付节点风险,避免覆盖已有经营提醒。

### 验证情况

- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 代码检查:已运行 `git diff --check`,通过。
- 浏览器回归:
  - 访问 `/dashboard/home`,确认“私域经营看板”正常出现。
  - 确认首页“经营风险预警”出现私域风险项。
  - 确认原有网销/渠道等风险仍保留。
  - 浏览器控制台错误日志为 0。

### 下一步建议

- A: 把“已报价/已成交”跟进结果接入报价单和提单。
- B: 做任务中心的私域回填闭环。
- C: 继续检查首页之外的驾驶舱,把私域风险接入驾驶舱。

---

## 2026-06-07 Codex 私域客户详情真实时间线

### 本次任务

持续开发第 2 轮:把私域客户详情抽屉里的静态时间线替换为真实业务时间线,统一展示客户入库、工商核验、跟进记录、交付包和任务节点。

### 修改文件

- `zhehang-erp-ui/src/api/private-domain.ts`
- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 新增私域客户时间线类型 `PrivateTimelineItem`,覆盖客户入库、工商核验、跟进、任务、交付包五类事件。
- 新增 `privateDomainApi.getContactTimeline(id)`,按客户 ID 聚合私域客户、工商核验、跟进记录、交付包和任务中心数据。
- 详情抽屉时间线从静态三条文案改为真实事件流。
- 打开客户详情时自动加载该客户时间线。
- 工商核验、生成跟进任务、保存跟进记录、生成交付包后会刷新详情时间线。
- 时间线增加状态标签和颜色点位,可区分已核验、已联系、已报价、已成交、待处理、逾期等状态。

### 验证情况

- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 代码检查:已运行 `git diff --check`,通过。
- 浏览器回归:
  - 访问 `/leads/private-domain`,进入“客户雷达”。
  - 点击第一条客户“详情”,确认详情抽屉正常打开。
  - 确认时间线出现“私域客户入库”和跟进相关事件。
  - 确认旧静态“工商/撞单判断”不再出现。
  - 浏览器控制台错误日志为 0。

### 下一步建议

- A: 将“已报价/已成交”的跟进记录接入报价单和提单。
- B: 把任务中心里的私域跟进任务做成可回填结果的工作流。
- C: 首页老板看板继续补“今日未跟进高意向客户”和“超时未复联”。

---

## 2026-06-07 Codex 私域跟进结果闭环

### 本次任务

持续开发第 1 轮:补齐私域客户从“生成跟进任务”到“记录跟进结果”的闭环,让电销/私域/销售每天能沉淀联系结果、报价金额、下次动作和客户阶段变化。

### 修改文件

- `zhehang-erp-ui/src/api/private-domain.ts`
- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 新增私域跟进记录模型,覆盖客户、负责人、跟进方式、跟进结果、跟进内容、报价金额、下次动作和下次跟进时间。
- 新增跟进结果枚举:无响应、已联系、有意向、已报价、已成交、暂缓、流失。
- 新增跟进 API:支持创建跟进记录、读取跟进台账,并按结果自动更新客户阶段。
- 记录跟进后自动更新客户最近互动、触达次数、下一动作、报价金额和标签。
- 对已联系/有意向/报价/成交的客户,自动把匹配的待办跟进任务标为完成。
- 私域运营页新增“跟进记录”页签,展示跟进次数、报价金额、成交记录、待复联数量和完整记录台账。
- 客户雷达和详情抽屉新增“记录跟进”入口。
- 新增“记录私域跟进”弹窗,用下拉、数字框和输入框完成一次跟进记录,减少手工录入成本。
- 首页私域老板指标中“私域客户”趋势补充跟进次数。

### 验证情况

- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 代码检查:已运行 `git diff --check`,通过。
- 浏览器回归:
  - 访问 `/leads/private-domain`,确认“跟进记录”页签存在。
  - 点击“客户雷达”,确认每条客户出现“记录跟进”按钮。
  - 点击第一条客户“记录跟进”,确认“记录私域跟进”弹窗正常出现。
  - 填写跟进内容并保存,确认自动切到“跟进记录”页签,台账出现新记录和跟进摘要。
  - 浏览器控制台错误日志为 0。

### 下一步建议

- A: 把“报价/成交”跟进结果进一步接到报价单、提单和财务核对。
- B: 给客户详情增加“最近跟进时间线”,把跟进记录、工商核验、交付包放在同一条时间轴。
- C: 给任务中心补“私域跟进任务来源”和“跟进结果回填”。

---

## 2026-06-07 Codex 私域规则、企微配置、交付包与老板看板

### 本次任务

承接上轮 A/B/C/D 计划,一次性补齐私域运营的归属规则配置、企业微信接入参数、成交后交付任务包和首页老板视角指标。

### 修改文件

- `zhehang-erp-ui/src/api/private-domain.ts`
- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `zhehang-erp-ui/src/views/dashboard/home.vue`
- `开发交接记录.md`

### 完成内容

- 新增私域归属规则模型,覆盖来源部门、首添人、保护期、首次触达时限、回收天数、撞单策略和优先级。
- 私域运营页新增“归属规则”页签,支持通过开关、下拉、数字框和输入框维护 7 类私域来源规则。
- 新增企业微信接入配置模型,覆盖 CorpId、客户联系 Secret、客户群 Secret、回调 URL、Token、AESKey、外部联系人、客户群、标签和互动记录同步范围。
- 私域运营页“接入配置”新增企微接入表单,保存后同步更新企业微信接入卡片状态。
- 新增成交交付包模型和生成逻辑,按代理记账、财税、地址挂靠、同行渠道、工商注销等业务线自动拆出交付任务。
- 客户雷达和客户详情抽屉新增“交付包/生成交付包”入口,生成后客户自动进入已成交阶段,任务写入跟进任务列表。
- 私域运营页新增“交付包”页签,展示交付包、任务数、待推进数量、最晚节点和任务拆解。
- 首页新增“私域经营看板”,展示私域客户、工商核验率、撞单风险和成交交付包,并把私域指标写入欢迎摘要与关键业务进展。
- 落地检查项更新为新的实际状态:分配与保护期、成交后交付、老板经营看板均能反映本轮功能。

### 验证情况

- 已按交接要求先运行 `git log --oneline -5` 查看最近提交。
- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 已启动前端开发服务: `http://localhost:3000/`。
- 浏览器回归:
  - 访问 `/leads/private-domain`,确认“私域运营”“归属规则”“客户雷达”“交付包”“接入配置”正常出现。
  - 点击“归属规则”,确认来源部门、保护期、撞单策略和“保存规则”正常出现,并保存第一条规则成功。
  - 点击“客户雷达”,点击第一条客户“交付包”,确认自动生成成交交付包并切到“交付包”页签。
  - 点击“接入配置”,确认企业微信接入参数、CorpId、客户联系 Secret、回调 URL、互动记录和“保存企微配置”正常出现,保存空配置成功。
  - 访问 `/dashboard/home`,确认“私域经营看板”“工商核验率”“撞单风险”“成交交付包”和“打开私域运营”正常出现。
  - 浏览器日志里有一条 dev server 启动前留下的 Vite 动态导入旧错误;页面当前可正常渲染与操作。

### 下一步建议

- A: 把本地私域配置落库到后端,做真实租户隔离和权限控制。
- B: 接真实企业微信 API/回调,同步外部联系人、客户群、标签和互动记录。
- C: 把私域交付包接到提单、财务核对、报税日历和回款节点。
- D: 增加归属规则命中率、撞单仲裁结果和交付逾期率报表。

---

## 2026-06-07 Codex 私域工商核验与撞单拦截

### 本次任务

继续推进私域运营落地,把私域客户导入和客户雷达接到现有企业主体库能力上,实现公司名工商核验、风险标签、勾稽摘要和撞单风险提示。

### 修改文件

- `zhehang-erp-ui/src/api/private-domain.ts`
- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 私域客户新增工商核验结构,保存主体 ID、统一社会信用代码、法人、注册资本、成立日期、工商状态、税务资质、风险标签、勾稽摘要和推荐动作。
- 批量导入预览阶段强制调用 `enterpriseApi.resolveCompany`,对每一行公司名称做工商主体核验。
- 导入预览新增“工商命中”统计和“工商/撞单”列,展示已核验/待核验、未重复/疑似重复/强命中、信用代码或勾稽摘要。
- 导入时会把工商核验结果写回私域客户,同步标签、评分、主体 ID、信用代码和撞单推荐动作。
- 对强命中撞单的导入行转为重复状态,不进入有效导入行,避免同一客户重复分配。
- 客户雷达新增“工商/撞单”列和“工商核验”按钮,支持单个私域客户重新核验并写回结果。
- 详情抽屉新增工商核验卡,展示主体来源、置信度、法人、注册资本、税务资质、成立日期、勾稽摘要和推荐服务。
- 落地检查项从“私域待接入工商核验”更新为“网销线索和私域导入均已接入工商核验”。

### 验证情况

- 已按交接要求先运行 `git log --oneline -5` 查看最近提交。
- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 浏览器回归:
  - 访问 `/leads/private-domain`,确认页面正常加载,控制台错误为 0。
  - 点击“客户雷达”,确认出现“工商/撞单”列和“工商核验”按钮。
  - 点击第一条客户“工商核验”,确认出现“工商核验完成”提示,控制台错误为 0。
  - 点击“批量导入”,确认页面出现“工商命中”和“工商/撞单”预览列。
  - 浏览器自动化环境缺少虚拟剪贴板,无法稳定模拟长文本粘贴;导入预览逻辑已通过构建校验,上轮导入链路已浏览器跑通。

### 下一步建议

- A: 做私域归属规则配置,把来源部门、首添人、保护期、回收规则和撞单优先级做成可配置。
- B: 接企业微信配置页,准备外部联系人、客户群、标签和互动记录同步字段。
- C: 私域成交后自动生成财税交付任务包,打通成交到交付闭环。
- D: 给老板驾驶舱增加私域有效线索、工商命中率、撞单率、成交金额和回款漏斗。

---

## 2026-06-07 Codex 私域客户批量导入模板

### 本次任务

根据用户选项 A,先把“私域客户 Excel 导入模板”落地,让企业微信、微信群、个人微信、朋友圈、公众号、视频号和老客转介绍客户可以批量进入私域客户雷达。

### 修改文件

- `zhehang-erp-ui/src/api/private-domain.ts`
- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 私域 API 新增导入模板字段、示例数据、导入行类型、预览行类型和导入结果类型。
- 新增私域客户导入校验:公司名称、联系人、手机号、来源触点、负责人必填;手机号格式校验;阶段值校验。
- 新增按“公司名称 + 手机号”的去重逻辑,同时识别历史客户重复和本次文件内重复。
- 新增 CSV 模板下载,带 UTF-8 BOM,可直接用 Excel 打开。
- 私域运营页新增“批量导入”页签,包含模板字段说明、CSV 文件选择、Excel 粘贴导入、预览统计和问题提示。
- 支持只导入“可导入”行,导入后自动回到客户雷达并刷新客户列表。
- 导入后的客户会补齐来源、触点名称、负责人、业务线、预估金额、标签、阶段、最近互动时间和下一步动作,后续可继续入库网销线索或生成跟进任务。

### 验证情况

- 已按交接要求先运行 `git log --oneline -5` 查看最近提交。
- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 浏览器回归:
  - 访问 `/leads/private-domain`,确认可直接进入私域运营页面。
  - 点击“批量导入”,确认出现“Excel 导入模板”“粘贴导入”“导入有效行”和模板字段表。
  - 粘贴 1 条新客户数据,确认预览统计为 1 行、可导入 1、重复 0、错误 0。
  - 点击“导入有效行”,确认客户进入“客户雷达”,并能看到公司、联系人、手机号、来源、标签、阶段、需求和下一步动作。
  - 点击“下载模板”,确认出现模板下载成功提示。
  - 浏览器控制台错误日志为 0。

### 下一步建议

- A: 私域入库强制接入工商核验和撞单管理,公司名称填完后自动补工商信息。
- B: 把私域归属规则、保护期、回收规则、撞单优先级做成系统配置。
- C: 做企业微信接入配置,同步外部联系人、客户群、标签和互动记录。
- D: 私域成交后自动生成财税交付任务包,打通成交到交付闭环。

---

## 2026-06-07 Codex 私域运营落地化与选项式问诊

### 本次任务

根据用户反馈“软件内容不落地、打字慢”,先对系统现状做业务落地审计,并把私域运营页从演示客户表升级为可执行的财务公司私域运营工作台。后续问用户问题统一改为选项式,减少打字成本。

### 修改文件

- `zhehang-erp-ui/src/router/routes.ts`
- `zhehang-erp-ui/src/api/private-domain.ts`
- `zhehang-erp-ui/src/views/leads/private-domain.vue`
- `开发交接记录.md`

### 完成内容

- 新增私域运营前端 API,覆盖私域客户、社群、内容触达、跟进任务、接入配置、运营画像、落地检查项和每日岗位动作。
- 新增 `/leads/private-domain` 页面和 `/scrm/private-domain` 历史入口重定向。
- 私域页新增“经营闭环”区,把私域触点 -> 公司核验 -> 查重分配 -> 销售跟进 -> 报价提单 -> 财税交付 -> 回款续费串起来。
- 新增 6 张每日岗位动作卡:私域运营、网销运营、电销坐席、渠道经理、销售顾问、财税交付,每张都带来源、目标和处理入口。
- 新增“落地问诊”默认 Tab,支持公司画像、私域触点、关键业务、参与岗位、入库必填字段、每日线索目标和交接规则。
- 将问诊问题改成 4 组选择题,用户可直接点选来源、归属规则、老板关注指标和数据进入方式。
- 私域客户可继续入库网销线索、生成跟进任务、标记意向,并增加加载/防重复点击/错误提示。

### 验证情况

- 已按交接要求先运行 `git log --oneline -5` 查看最近提交。
- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 浏览器回归:
  - 启动前端 dev server 后访问 `/leads/private-domain`,重新登录开发预览账号进入页面。
  - 确认默认 Tab 为“落地问诊”,页面出现经营闭环、4 张指标卡、6 张每日岗位动作卡、4 张接入卡和 24 个选择题选项。
  - 点击“企微/微信群为主”并保存运营画像,确认保存成功提示出现,选项状态正确保留。

### 下一步建议

- P1: 把私域运营画像继续拆成真正配置项:来源归属、保护期、回收规则、撞单优先级、服务线字段必填规则。
- P2: 做 Excel 导入模板,先让企业微信/微信群/个人微信客户能批量进入私域客户雷达。
- P3: 将私域入库线索强制接入工商核验、撞单管理和分配规则,避免重复客户和漏跟进。
- P4: 成交后按服务项自动生成财税交付任务包,形成私域 -> 成交 -> 交付 -> 续费闭环。

---

## 2026-06-04 Codex 分配规则统一收口

### 本次任务

收尾分配配置页改造,把线索来源路由、权重算法、承接团队、公海局部规则和历史分配日志统一收口到系统分配规则中枢,避免公海页面和系统配置重复维护。

### 修改文件

- `zhehang-erp-ui/src/router/routes.ts`
- `zhehang-erp-ui/src/views/system/distribute-config.vue`
- `zhehang-erp-ui/src/views/crm/lead.vue`
- `开发交接记录.md`

### 完成内容

- 分配配置页新增 4 张经营概览卡,汇总启用来源、今日入库、入 CRM 和成交线索。
- 渠道路由页补齐探迹 T+1 新设企业、经营异常解除、百度/抖音表单、私域、同行地址挂靠、代理记账断新开、老客户转介绍和回收池再激活等来源规则。
- 渠道路由表新增启用状态、来源类型、承接团队、今日入库、CRM/成交、响应 SLA、优先级和说明,并补充“入库 -> 防撞 -> 路由 -> 权重分配 -> 回收预警”链路说明。
- 权重配置补齐职级基础权重、分配模式、抢单规则默认值和飞书浅色办公风覆盖。
- 分配日志补齐 5 条可追溯样例,展示自动分配、主管手动、抢单、转移等场景和权重明细。
- CRM 旧公海规则弹窗增加“主分配规则统一在系统配置维护”的提示和跳转入口。
- 新增旧路径 `/crm/lead` 到 `/customer/company-pool` 的隐藏重定向,避免历史入口进入空白旧页。

### 验证情况

- 已按交接要求先运行 `git log --oneline -5` 查看最近提交。
- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 浏览器回归:
  - 访问 `/system/distribute-config`,确认分配配置页正常加载,出现 4 张概览卡、3 个标签和权重配置内容,控制台错误为 0。
  - 切换“渠道路由”,确认 5 步路由链路、8 条渠道规则和“分配配置是唯一主配置”提示正常展示,控制台错误为 0。
  - 切换“分配日志”,确认 5 条分配日志和分页统计正常展示,控制台错误为 0。
  - 访问旧路径 `/crm/lead`,确认自动重定向到 `/customer/company-pool`,不再进入空白旧页,控制台错误为 0。

### 下一步建议

- P1: 将分配配置中的渠道路由保存接口正式落到后端,避免继续只做本地演示保存。
- P2: 把 `/customer/company-pool` 的公海管理入口进一步关联到 `/system/distribute-config` 和 `/system/recycle-config`,减少重复规则入口。
- P3: 逐步清理 `crm/lead.vue` 等旧 CRM 页面,确认无动态菜单依赖后删除或迁移到当前客户中心主入口。

---

## 2026-06-04 Codex 外呼任务经营勾稽优化

### 本次任务

继续优化当前呼叫中心外呼任务页,把外呼从单纯“拨打任务”推进为可看名单来源、CRM 转化、商机、成交和 ROI 的业务闭环页面。

### 修改文件

- `zhehang-erp-ui/src/api/call-center.ts`
- `zhehang-erp-ui/src/views/call-center/outbound.vue`
- `开发交接记录.md`

### 完成内容

- `OutboundTask` 新增可选经营字段:线索来源、名单批次、目标客群、业务线、筛选条件、负责团队、获客成本、预计收入、入 CRM 数、商机数、成交数和待跟进数。
- 外呼 mock 样例补齐探迹新注册企业、经营异常解除、代理记账续费、同行渠道地址挂靠四类业务场景。
- 外呼任务列表新增“转化”和“ROI”列,可直接扫到 CRM、商机、成交和投产比。
- 外呼任务详情新增“名单与转化勾稽”区,展示名单规则、目标客群、转化漏斗、成本收入、ROI 和待跟进数量。
- 详情时间线和底部摘要补充 CRM 转化节点,让任务状态和业务结果在同一抽屉里闭环。

### 验证情况

- 已按交接要求先运行 `git log --oneline -5` 查看最近提交。
- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 浏览器回归:
  - 访问 `/call-center/outbound`,重新登录开发预览账号后确认列表出现“转化”“ROI”列,第一条任务显示 `CRM 52 / 24 商机 / 7 成交 / 16.3x`。
  - 点击第一条“详情”,确认统一详情抽屉出现“名单与转化勾稽”,包含探迹新注册企业池、T+1 新设企业批次、筛选条件、转化漏斗、预计收入 `¥43,800` 和 ROI `16.3x`。
  - 浏览器错误日志为 0。

### 下一步建议

- P1: 将外呼任务和客户中心的线索/企业主体真正联动,点击“入 CRM/商机”能跳转到对应客户或商机详情。
- P2: 给通话记录详情补 AI 摘要、意向等级、加 CRM 结果和通话转写。
- P3: 给网销 ROI 页和外呼任务建立同一套成本口径,避免投放成本、名单成本、通话成本重复计算。

---

## 2026-06-04 Codex 详情页模板统一第六轮

### 本次任务

继续优化详情页内容和模板 UI 样式一致化。本轮聚焦呼叫中心,将通话记录详情和外呼任务详情迁移到公共 `BusinessDetailDrawer`,并顺手补齐呼叫中心动态路由和本地 mock 数据,避免页面存在但无法访问、无法点开详情。

### 修改文件

- `zhehang-erp-ui/src/router/routes.ts`
- `zhehang-erp-ui/src/api/call-center.ts`
- `zhehang-erp-ui/src/views/call-center/call-record.vue`
- `zhehang-erp-ui/src/views/call-center/outbound.vue`
- `开发交接记录.md`

### 完成内容

- 新增一级动态路由 `/call-center`,挂入实时监控、通话记录、外呼任务、坐席管理、技能组、号码与线路、IVR 流程和通话报表。
- 呼叫中心 mock 数据从空数组补齐为财税电销场景样例,包含坐席、技能组、SIP 线路、号码池、IVR 流程、通话记录、外呼任务和监控趋势。
- 通话记录详情迁移到统一详情抽屉,统一展示通话 ID、方向、主被叫、坐席、技能组、挂机方、通话时长、录音回放、质检评分和流转记录。
- 外呼任务详情迁移到统一详情抽屉,统一展示任务状态、主叫号码、总数/已拨/接通/失败、完成度、实时统计、拨打明细和任务流转记录。
- 详情页底部操作继续保留原业务动作:通话详情可清空/提交质检,外呼任务可暂停、停止、启动/恢复。

### 验证情况

- 已按交接要求先运行 `git log --oneline -5` 查看最近提交。
- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import './feishu.scss'` 弃用警告,不影响构建。
- 浏览器回归:
  - 访问 `/call-center/call-record`,列表显示 8 条样例通话;点击第一条“详情”,统一详情抽屉正常打开,检测到录音回放、质检评分、5 条流转记录和底部提交质检动作。
  - 访问 `/call-center/outbound`,列表显示 4 条样例任务;点击第一条“详情”,统一详情抽屉正常打开,检测到完成度面板、实时统计、拨打明细、流转记录和暂停/停止动作。

### 遗留问题

- 消息中心详情/设置抽屉是飞书消息专用结构,本轮未强行迁移为业务详情模板。
- 呼叫中心部分页面仍偏“深色科技风/大卡片风”,后续可继续向统一浅色办公风格收敛。
- 全局 Sass `@import` 弃用警告仍存在,后续可单独迁移到 `@use`。

### 下一步建议

- P1: 继续统一呼叫中心其余详情/弹窗视觉,尤其坐席、技能组、号码与线路、IVR 流程。
- P2: 为通话记录补 AI 摘要、意向等级、加 CRM 结果和关联客户/线索跳转。
- P3: 为外呼任务补名单批次、筛选条件、ROI 归因、商机转化与通话明细勾稽。

---

## 2026-06-04 Codex 详情页模板统一第五轮

### 本次任务

继续优化每个详情页的内容和模板 UI 样式一致化。本轮聚焦订单合同主链路,将订单提单详情和合同详情迁移到公共 `BusinessDetailDrawer`,让提单、合同与前几轮客户/任务/财务详情保持同一套阅读结构。

### 修改文件

- `zhehang-erp-ui/src/views/order/bill.vue`
- `zhehang-erp-ui/src/views/order/contract.vue`
- `开发交接记录.md`

### 完成内容

- 订单提单详情迁移到统一详情抽屉,统一展示订单编号、客户、提交时间、预计签约、折后金额、待收金额、付款方式、确认方式、提成比例和参考提成。
- 提单详情保留审批链路、超期提示、服务子项表、特殊约定、驳回说明和重新提交动作,流转记录统一为详情模板时间线。
- 合同详情迁移到统一详情抽屉,统一展示合同编号、关联订单、模板、创建时间、甲乙方、签署方式、合同金额和服务期限。
- 合同详情保留签署信息、电子签署提示、到期管理、续签意向、跟进人、合同版本历史、联动事件和下载/确认签署动作。

### 验证情况

- 已按交接要求先运行 `git log --oneline -5` 查看最近提交。
- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import` 弃用警告,不影响构建。
- 浏览器回归:
  - 使用开发预览账号 `admin / admin123` 登录后访问 `/order/bill`,点击第一条“详情”,统一详情抽屉正常打开,检测到流程状态、服务子项和流转记录。
  - 访问 `/order/contract`,点击第一条“查看”,统一详情抽屉正常打开,检测到签署信息、到期管理和流转记录。

### 遗留问题

- 呼叫中心通话详情和外呼任务详情仍是旧抽屉,下一轮建议继续迁移。
- 消息中心详情/设置抽屉是飞书消息专用结构,暂不强行改成业务详情模板,后续只统一视觉细节。
- 当前本地 Vite 服务绑定在 `localhost:3000`;`127.0.0.1:3000` 在本机环境下可能拒绝连接,预览建议使用 `http://localhost:3000`。

### 下一步建议

- P1: 迁移呼叫中心通话记录详情,统一展示录音、转写、AI 摘要、外呼结果和加 CRM 记录。
- P2: 迁移外呼任务详情,统一展示任务筛选条件、线索批次、触达结果、转化状态和通话明细。
- P3: 清理已迁移页面遗留的旧详情抽屉样式,减少 CSS 冗余。

---

## 2026-06-04 Codex 详情页模板统一第四轮

### 本次任务

继续优化每个详情页的内容和模板一致性。本轮聚焦客户线索侧的个人客户池和撞单管理,将原有差异较大的详情抽屉迁移到公共 `BusinessDetailDrawer`,并补齐本地演示样例,方便没有真实接口时也能打开详情验证。

### 修改文件

- `zhehang-erp-ui/src/views/leads/personal-pool.vue`
- `zhehang-erp-ui/src/views/leads/collision-manage.vue`
- `开发交接记录.md`

### 完成内容

- 个人客户池详情迁移到统一模板,统一展示客户来源、等级、阶段、联系人、保护期、跟进频率、健康状态、服务订单/备注和跟进流转记录。
- 保留个人池原有写跟进、转移客户、退回公海动作,统一放入详情底部动作区。
- 个人客户池在接口为空时补 3 条演示客户,避免本地预览无法点开详情。
- 撞单管理详情迁移到统一模板,统一展示撞单编号、匹配字段、冲突双方、冲突级别、处理结果、双方跟进对比和仲裁流转记录。
- 保留撞单原有“立即处理/确认处理”动作,待处理撞单仍可从详情底部进入仲裁弹窗。
- 撞单管理补 3 条本地演示撞单记录,覆盖待处理、已处理、跨渠道重复、抢单冲突等状态。

### 验证情况

- 已按交接要求先运行 `git log --oneline -5` 查看最近提交。
- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import` 弃用警告,不影响构建。
- 浏览器回归:
  - `/leads/personal-pool` 会重定向到 `/customer/personal-pool`;列表有 3 条演示客户,点击详情后统一详情抽屉正常打开,检测到 8 个关键信息块、5 条流转记录、跟进健康度和联系人模块,浏览器错误日志为空。
  - `/leads/collision-manage` 会重定向到 `/customer/collision-manage`;列表有 3 条演示撞单,点击“查看详情”后统一详情抽屉正常打开,检测到 8 个关键信息块、3 条流转记录、双方对比和立即处理动作,浏览器错误日志为空。

### 遗留问题

- 仍有复杂详情抽屉未迁移:呼叫中心通话详情、外呼任务详情、订单提单详情、合同详情。
- 消息中心详情和设置抽屉目前是飞书消息专用结构,可暂时保留,后续只统一视觉细节而不强行改成业务详情模板。
- 部分页面仍是深色大卡风格,后续可继续按统一浅色办公风格做页面整体统一。

### 下一步建议

- P1: 迁移订单提单详情和合同详情,统一订单合同主链路的详情阅读体验。
- P2: 迁移呼叫中心通话记录和外呼任务详情,将录音、AI 摘要、通话转写、外呼结果沉淀为统一详情模板专用内容块。
- P3: 将公共详情组件继续扩展附件、评论、审批轨迹和业务关联卡插槽。

---

## 2026-06-04 Codex 详情页模板统一第三轮

### 本次任务

继续推进各页面详情页模板一致化。本轮聚焦任务中心的客户资料交接和提成结算,将两个高频业务详情接入公共 `BusinessDetailDrawer`,并补齐客户交接样例数据,方便页面直接演示和验证。

### 修改文件

- `zhehang-erp-ui/src/api/task-center.ts`
- `zhehang-erp-ui/src/views/task-center/handover.vue`
- `zhehang-erp-ui/src/views/task-center/commission.vue`
- `开发交接记录.md`

### 完成内容

- 客户资料交接详情迁移到统一详情抽屉,统一展示交接编号、合同、销售、会计、截止日期、必须项完成率、十项清单和交接流转记录。
- 保留客户交接原有销售/会计操作、退回、确认、交接完成和建账初始化联动,同时让样例资料状态和文件占位更贴近真实业务。
- 提成结算详情迁移到统一详情抽屉,统一展示结算单号、销售、订单、业绩金额、提成比例、奖金、扣回、实发金额和五步审核流转。
- 保留提成结算原有销售确认、主管审核、财务确认、老板审批、标记发放和上传凭证动作。
- `task-center` mock API 新增 3 条客户交接样例和 `handoverApi.ensureSamples`,解决本地空列表时无法演示详情的问题。

### 验证情况

- 已按交接要求先运行 `git log --oneline -5` 查看最近提交。
- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import` 弃用警告,不影响构建。
- 浏览器回归:
  - `/task-center/handover` 自动补齐 3 条样例,点击第一条“查看详情”后统一详情抽屉正常打开,检测到 8 个关键信息块、4 条流转记录、十项清单和交接完成按钮,浏览器错误日志为空。
  - 复验客户交接详情,已确认资料能展示“已提供”和 `.pdf` 文件占位,不再全部显示未操作。
  - `/task-center/commission` 点击第一条“详情”后统一详情抽屉正常打开,检测到 10 个关键信息块、5 条审核流转、金额汇总和审核流程,浏览器错误日志为空。

### 遗留问题

- 仍有复杂详情抽屉未迁移:呼叫中心通话详情、外呼任务详情、个人池客户详情、撞单详情、订单提单详情、合同详情。
- 公共详情抽屉的底部动作区是 sticky 布局,长内容页面可继续优化底部留白和滚动体验。
- `src/styles/index.scss` 的 Sass `@import` 弃用警告仍存在,不影响构建。

### 下一步建议

- P1: 继续迁移订单提单详情和合同详情,把订单、合同、回款、开票、退款这条主链路统一起来。
- P2: 迁移客户个人池、公海、撞单详情,统一客户主数据和防撞裁决阅读体验。
- P3: 迁移呼叫中心详情,将录音、AI 摘要、通话转写和外呼结果沉淀为详情模板专用内容块。

---

## 2026-06-04 Codex 详情页模板统一第二轮

### 本次任务

继续按用户反馈统一各模块详情页 UI,本轮将更多业务详情抽屉接入公共 `BusinessDetailDrawer`,并补充详情内容的关键指标、摘要、流转记录和底部动作。

### 修改文件

- `zhehang-erp-ui/src/views/leads/market-intelligence.vue`
- `zhehang-erp-ui/src/views/supply/vendor.vue`
- `zhehang-erp-ui/src/views/supply/purchase.vue`
- `zhehang-erp-ui/src/views/crm/customer.vue`
- `zhehang-erp-ui/src/views/org/employee.vue`
- `zhehang-erp-ui/src/views/task-center/once.vue`
- `zhehang-erp-ui/src/views/task-center/department.vue`
- `zhehang-erp-ui/src/views/task-center/boss.vue`
- `开发交接记录.md`

### 完成内容

- 拓客情报详情迁移到统一模板,新增选中情报汇总条,展示已选数量、A 级数量、预计金额、联系方式数量和批量入电销/入公海动作。
- 地址供应商详情迁移到统一模板,统一展示供应商编号、合作等级、可用地址、到期状态、合作信息、关联挂靠地址和资源补充历史。
- 资源补充单详情迁移到统一模板,统一展示资源成本、返点、实际付款、审批上架进度、地址资源明细和审批流。
- CRM 客户详情迁移到统一模板,保留联系人、跟进、商机、合同标签页,统一客户头部、关键资料和关联数据时间线。
- 员工详情迁移到统一模板,拆分为档案摘要、个人信息、任职合同、紧急联系人和任职时间线。
- 一次性任务、项目部任务、老板特殊任务详情迁移到统一模板,保留原任务动作,补充任务/项目摘要、进度、子任务、汇报与流转记录。

### 验证情况

- 已按交接要求先运行 `git log --oneline -5` 查看最近提交。
- 代码检查:已运行 `git diff --check`,通过。
- 前端构建:已运行 `npm run build`,通过。仅有既有 Sass `@import` 弃用警告,不影响构建。
- 浏览器回归:
  - `/leads/market-intelligence` 点击第一条企业详情,统一详情抽屉正常打开,检测到 6 个关键信息块、4 条流转记录,浏览器错误日志为空。
  - `/task-center/once` 点击第一条任务详情,统一详情抽屉正常打开,检测到 7 个关键信息块、3 条流转记录,浏览器错误日志为空。
  - `/supply/vendor`、`/supply/purchase`、`/hrm/employee` 当前本地数据为空,已确认页面无 Vite 错误叠层、无浏览器 error;详情抽屉依赖业务数据,本轮以构建和页面渲染验证为准。
  - `/crm/customer` 是隐藏旧路由,会重定向到 `/leads/workbench`;本轮保留代码迁移,以构建验证为准。

### 遗留问题

- 仍有复杂详情抽屉未迁移:呼叫中心通话详情、外呼任务详情、提成结算详情、交接详情、订单提单详情、合同详情、个人池客户详情、撞单详情。
- 部分页面当前本地数据为空,无法做点击详情的完整浏览器回归;后续补数据或接真实接口后继续验证。
- 公共详情模板后续可继续扩展附件、评论、审批流、录音转写、业务关联卡等标准插槽。

### 下一步建议

- P1: 继续迁移订单合同/账单详情,统一合同、提单、回款、开票、退款的详情阅读体验。
- P2: 迁移客户公海/个人池/撞单详情,统一客户主体详情与防撞裁决逻辑。
- P3: 迁移呼叫中心详情,将录音、AI 摘要、通话转写、外呼结果沉淀成统一详情模板的专用内容块。

---

## 2026-06-04 Codex 统一详情页模板第一轮

### 本次任务

按用户反馈,系统各页面详情页模板差距较大。本轮先参考飞书消息详情的结构,沉淀统一详情抽屉组件,并迁移一批高频详情页作为模板样例。

### 修改文件

- `zhehang-erp-ui/src/components/common/BusinessDetailDrawer.vue`
- `zhehang-erp-ui/src/views/finance/journal.vue`
- `zhehang-erp-ui/src/views/supply/channel-partner.vue`
- `zhehang-erp-ui/src/views/leads/enterprise-master.vue`
- `开发交接记录.md`

### 完成内容

- 新增公共 `BusinessDetailDrawer` 组件,统一详情抽屉的标题区、头像/缩写、状态标签、操作区、关键信息卡、内容卡、流转记录和底部按钮。
- 财务日记账详情迁移到统一模板:保留原可编辑表单,新增统一标题、金额/净额/归集信息卡和流转记录。
- 同行渠道详情迁移到统一模板:去掉原深色 hero,统一为浅色办公详情头、关键信息卡、近期开单和渠道流转记录。
- 企业工商税务档案详情迁移到统一模板:保留工商信息、联系方式、风险标签和税务档案面板,统一外壳和操作区。

### 验证情况

- 已按交接要求先运行 `git log --oneline -5` 查看最近提交。
- 前端构建:已运行 `npm run build`,通过。
- 浏览器回归:
  - `/finance/journal` 点击“展开”后统一详情抽屉正常打开,含 5 个关键信息块、2 条流转记录和底部操作区,控制台无 error。
  - `/supply/channel-partner` 点击“详情”后统一详情抽屉正常打开,含 8 个关键信息块、2 条流转记录和底部操作区,控制台无 error。
  - `/customer/enterprise-master` 页面无白屏、无 Vite 错误;当前浏览器数据下企业列表无可点详情行,本轮仅做构建与代码迁移验证。

### 遗留问题

- 这只是详情模板统一的第一轮,未一次性迁移所有复杂详情页。
- 订单详情、合同详情、撞单详情、公海客户详情、任务交接详情等页面体量较大,建议按公共组件继续分批迁移。
- `src/styles/index.scss` 的 Sass `@import` 弃用警告仍存在,不影响构建。

### 下一步建议

- P1: 迁移订单提单详情、合同详情、订单财务核对详情,这些是业务高频页。
- P2: 迁移客户/线索/撞单/公海详情,统一客户主数据的详情阅读体验。
- P3: 将公共详情组件扩展出标准插槽:审批流、附件、评论、关联业务、消息闭环。

---

## 2026-06-04 Codex 飞书消息处理闭环打磨

### 本次任务

按用户要求继续打磨飞书式消息中心,本轮把消息从“已读/未读”推进到“待处理/已处理/可追踪备注”的闭环。

### 修改文件

- `zhehang-erp-ui/src/api/notification.ts`
- `zhehang-erp-ui/src/components/MessageCenter.vue`
- `zhehang-erp-ui/src/views/system/notification.vue`
- `开发交接记录.md`

### 完成内容

- 消息模型新增 `isDone / doneTime / doneBy / doneRemark`,支持处理完成状态和处理结论。
- 消息 API 新增单条完成、重新打开、批量完成、批量重新打开能力,仍保持 localStorage 兜底。
- `/system/notification` 顶部新增“待处理/已处理”指标卡,左侧收件箱新增“待处理/已处理”入口。
- 消息卡片增加“已处理”状态、完成/重新打开按钮;已处理消息视觉弱化为绿色闭环状态。
- 批量工具条新增“标记处理完成/重新打开”,方便一次处理多条同类消息。
- 消息详情抽屉新增处理备注输入、处理人、处理时间和流转记录,能看到消息发起、已读、完成处理的过程。
- 顶部铃铛弹层显示待处理数量,方便在任意页面感知未闭环消息。

### 验证情况

- 已按交接要求先运行 `git log --oneline -5` 查看最近提交。
- 前端构建:已运行 `npm run build`,通过。
- 浏览器回归:
  - `/system/notification` 正常渲染,出现待处理/已处理指标和左侧入口,无 Vite 错误叠层,控制台无 error。
  - 勾选首条消息后批量条出现“标记处理完成”。
  - 打开消息详情后可见处理备注输入框、流转记录和“标记处理完成”动作。
  - 顶部铃铛弹层显示未读、待处理、加急和提醒状态,控制台无 error。

### 遗留问题

- 处理状态仍保存在 localStorage;后续接后端时建议拆成消息主表 + 用户处理状态表,避免多人处理互相覆盖。
- 当前“当前用户”是前端占位;接后端后应使用真实登录用户和审计时间。
- `src/styles/index.scss` 的 Sass `@import` 弃用警告仍存在,不影响构建。

### 下一步建议

- P1: 后端新增消息处理状态接口,支持单人/多人接收人维度的已读、稍后、归档、完成状态。
- P2: 把“标记处理完成”与业务页动作联动,例如订单核对完成、渠道账期处理、报税资料补齐后自动闭环消息。
- P3: 做“消息转任务/转审批/转协作讨论”,形成飞书式任务协作闭环。

---

## 2026-06-04 Codex 飞书消息设置与订阅打磨

### 本次任务

按用户要求继续打磨飞书式消息中心,本轮补齐“提醒控制、免打扰、业务订阅/静音”能力,降低内部消息噪音。

### 修改文件

- `zhehang-erp-ui/src/api/notification.ts`
- `zhehang-erp-ui/src/components/MessageCenter.vue`
- `zhehang-erp-ui/src/views/system/notification.vue`
- `开发交接记录.md`

### 完成内容

- 消息 API 新增 `NotificationPreferences`,支持 localStorage 持久化消息设置。
- 增加消息提醒总开关、桌面弹窗、提示音、免打扰时段、加急消息打断免打扰、每日摘要时间、按业务类型订阅/静音。
- `/system/notification` 顶部新增“消息设置”入口,打开飞书式设置抽屉。
- 消息列表增加提醒状态标签、免打扰标签、静音业务数量提示;被静音类型的消息卡片会显示“已静音”并弱化视觉。
- 业务类型补齐“系统通知”,现在系统、财务、客户、订单、渠道、税务、任务、审批、@我协作都能筛选和订阅。
- 顶部铃铛弹层接入消息设置状态,显示“提醒开启/免打扰中/提醒关闭”,并增加“设置”入口。

### 验证情况

- 已按交接要求先运行 `git log --oneline -5` 查看最近提交。
- 前端构建:已运行 `npm run build`,通过。
- 浏览器回归:
  - `/system/notification` 正常渲染,无 Vite 错误叠层,控制台无 error。
  - “消息设置”按钮可打开设置抽屉,包含提醒方式、免打扰与摘要、业务订阅 3 个区域。
  - 设置抽屉内检测到 9 类订阅行、15 个开关。
  - 顶部铃铛弹层正常打开,显示提醒状态,底部有“设置”和“查看全部”入口,控制台无 error。

### 遗留问题

- 设置仍为前端 localStorage 版本;后续接后端时要按用户维度、租户维度保存。
- 目前静音只影响提醒状态展示,不隐藏消息记录;后续接实时推送时应在推送层按设置过滤。
- `src/styles/index.scss` 的 Sass `@import` 弃用警告仍存在,不影响构建。

### 下一步建议

- P1: 建后端 `message_preference` 表,按用户保存提醒设置。
- P2: 实现消息触发规则时读取订阅/免打扰配置,决定是否推送、是否只入库。
- P3: 增加飞书式“消息分组规则/关键词订阅/@我聚合/审批待办聚合”。

---

## 2026-06-04 Codex 飞书消息中心继续打磨

### 本次任务

按用户要求继续打磨飞书式消息体验,重点把消息中心从“能看能跳转”推进到“能整理、能批量处理、能快速扫”。

### 修改文件

- `zhehang-erp-ui/src/api/notification.ts`
- `zhehang-erp-ui/src/components/MessageCenter.vue`
- `zhehang-erp-ui/src/views/system/notification.vue`
- `开发交接记录.md`

### 完成内容

- 消息 API 增加批量已读、批量未读、批量稍后、取消稍后、批量归档、批量恢复和批量删除能力,仍保持 localStorage 兜底。
- `/system/notification` 消息列表改成按时间分组展示,增加“今天/昨天/近 7 天/更早”分隔线,扫消息更接近飞书收件箱。
- 将原来只表示未读的禁用复选框改成真实可选控件,新增本页全选、已选数量、批量已读/未读/稍后/归档/恢复/删除/取消选择。
- 消息卡片新增选中态和未读蓝点,保留点击打开详情、业务跳转、星标、稍后、归档等原动作。
- 顶部铃铛弹层增加每条消息的星标、稍后、标已读和业务处理快捷操作,不用进入完整页面也能先整理消息。

### 验证情况

- 已按交接要求先运行 `git log --oneline -5` 查看最近提交。
- 前端构建:已运行 `npm run build`,通过。
- 浏览器回归:
  - `/system/notification` 正常渲染,8 条消息卡片、2 个时间分组、8 个可选复选框,无 Vite 错误叠层,控制台无 error。
  - 勾选首条消息后批量条显示“已选 1 条”,点击“取消选择”后恢复提示态。
  - 顶部铃铛弹层正常打开,6 条消息卡片均出现快捷动作区,控制台无 error。

### 遗留问题

- 仍是前端 localStorage 版本,后续需要接后端消息表、接收人、已读回执、实时推送和业务触发规则。
- `src/styles/index.scss` 的 Sass `@import` 弃用警告仍存在,不影响构建。

### 下一步建议

- P1: 建后端消息表与接收人表,把当前批量动作迁移到真实接口。
- P2: 接入业务触发:日记账待归集、渠道超账期、订单核对、报税截止、撞单裁定、ROI 预警。
- P3: 增加飞书式消息订阅设置、免打扰、@我聚合、审批/任务联动和 WebSocket/SSE 实时推送。

---

## 2026-06-04 Codex 打磨营销 ROI 与活动管理体验

### 本次任务

继续打磨当前 `/leads/online-roi` 相关内容,把线上运营 ROI 与营销活动获客成本管理做得更统一、更可用。

### 修改文件

- `zhehang-erp-ui/src/api/marketing.ts`
- `zhehang-erp-ui/src/views/marketing/campaign.vue`
- `zhehang-erp-ui/src/views/supply/channel-cost.vue`
- `开发交接记录.md`

### 完成内容

- 营销活动 API 增加 `R{data}` 解包、静默请求失败处理和 localStorage 兜底,后端不可用时页面仍能演示活动、预算、花费、曝光、点击、线索和 CAC。
- `/leads/campaign` 重做为飞书式浅色活动 ROI 页面:补总投放、累计线索、平均 CAC、最佳活动、效率分层、筛选、重点活动卡片、活动明细表、编辑弹窗和实时指标预览。
- 活动 ROI 计算补充点击率、转线索率、预算使用率、效率等级,方便网销团队按 CAC 和预算使用做投放复盘。
- `/leads/online-roi` 统一浅色头部视觉,移除深色渐变孤岛,并新增“营销活动”入口,让网销投产比和活动 ROI 两个页面能互相跳转。

### 验证情况

- 已按交接要求先运行 `git log --oneline -5` 查看最近提交。
- 前端构建:已运行 `npm run build`,通过。
- 浏览器回归:
  - `/leads/campaign` 正常渲染,无 Vite 错误叠层,控制台无 error。
  - `/leads/online-roi` 正常渲染,头部为浅色白底,存在“营销活动”入口,控制台无 error。
  - 从 `/leads/online-roi` 点击“营销活动”成功跳转到 `/leads/campaign`。

### 遗留问题

- 当前活动 ROI 页面已兼容真实接口,但真实后端只返回已有活动时会以真实数据为准;后续需要把网销投产比、营销活动、CRM 线索的 `campaignId` 关联再做深。
- `src/styles/index.scss` 的 Sass `@import` 弃用警告仍存在,不影响构建。

### 下一步建议

- 把投放活动和网销渠道成本统一成一个数据源,避免同一笔费用在 `/leads/online-roi` 与 `/leads/campaign` 两边重复录入。
- 在线索表增加活动归因展示,让“活动 -> 线索 -> 成交 -> 回款 -> ROI”完整勾稽。
- 增加月度投放复盘页,按渠道、活动、销售归属、客户类型拆 CAC 和成交 ROI。

---

## 2026-06-04 Codex 飞书式消息中心 P0

### 本次任务

按用户要求先实现一版“马上能用”的飞书式消息中心:顶部消息红点、业务消息卡片、已读/未读、星标、稍后、归档、点击跳转业务页面。

### 修改文件

- `zhehang-erp-ui/src/api/notification.ts`
- `zhehang-erp-ui/src/components/MessageCenter.vue`
- `zhehang-erp-ui/src/views/system/notification.vue`
- `zhehang-erp-ui/src/router/routes.ts`
- `开发交接记录.md`

### 完成内容

- 将原通知接口升级为本地可持久化的业务消息源,内置财务、客户、订单、渠道、税务、任务、审批、@我等示例业务消息。
- 顶部铃铛升级为飞书式消息弹层:未读红点、加急统计、全部/未读/稍后/星标分组、紧凑消息卡片、点击跳转业务页。
- `/system/notification` 重做为消息中心页面:左侧收件箱与业务类型筛选,右侧业务消息卡片流。
- 支持已读/标未读、星标、稍后处理、归档/恢复、删除、重置演示数据、搜索和优先级过滤。
- 路由菜单标题由“通知公告”调整为“消息中心”。
- 当前版本不依赖后端消息表,先用本地数据跑通体验;后续可接后端消息表、触发规则和 WebSocket/SSE。

### 验证情况

- 已按交接要求先运行 `git log --oneline -5` 查看最近提交。
- 前端构建:已运行 `npm run build`,通过。
- 浏览器验证:
  - `/system/notification` 页面正常渲染,8 条消息卡片、7 条未读、3 条加急,无 Vite 错误叠层,控制台无 error。
  - `/dashboard/home` 顶部铃铛弹层正常打开,显示 6 条消息卡片和未读红点。
  - 弹层首条消息点击后成功跳转 `/leads/online-roi`,未读数从 7 变为 6。

### 遗留问题

- 本轮是 P0 可用版,消息数据暂用 localStorage;后续需要接后端消息表、接收人、权限隔离、业务触发规则和实时推送。
- 开始本轮时工作区干净;开发过程中出现一组与本轮无关的营销活动/ROI 未提交改动,本轮未混入提交,需后续单独确认。
- `src/styles/index.scss` 的 Sass `@import` 弃用警告仍存在,不影响构建。

### 下一步建议

- P1: 接入后端消息表和接收人模型,把 localStorage 兜底切到真实接口。
- P2: 把日记账、渠道账期、报税提醒、撞单、订单财务核对接入自动触发消息。
- P3: 做 WebSocket/SSE 实时推送、已读回执、@协作讨论流。

---

## 2026-06-04 Codex 统一飞书风格页面视觉

### 本次任务

按用户要求,把系统页面整体调整为更接近飞书/Lark 的浅色办公风格,并接收工作区中未提交的客户税务档案功能,完成构建与浏览器回归后提交。

### 修改文件

- `zhehang-erp-ui/src/styles/variables.scss`
- `zhehang-erp-ui/src/styles/element.scss`
- `zhehang-erp-ui/src/styles/index.scss`
- `zhehang-erp-ui/src/styles/feishu.scss`
- `zhehang-erp-ui/src/components/layout/Sidebar.vue`
- `zhehang-erp-ui/src/components/layout/Header.vue`
- `zhehang-erp-ui/src/components/layout/MainLayout.vue`
- `zhehang-erp-ui/src/api/crm.ts`
- `zhehang-erp-ui/src/components/TaxProfilePanel.vue`
- `zhehang-erp-ui/src/views/leads/enterprise-master.vue`
- `zhehang-erp-server/zhehang-erp-modules/src/main/java/com/zhehang/erp/modules/crm/**/CrmTaxProfile*`
- `zhehang-erp-db/migration/V8_crm_tax_profile.sql`
- `开发交接记录.md`

### 完成内容

- 全局设计变量切换为飞书式浅色办公色板:白色侧栏、浅灰内容背景、蓝色主色、低饱和边框与阴影。
- 新增 `feishu.scss` 作为统一视觉覆盖层,收敛旧页面中的深色、金色、重渐变、重阴影和不一致表格/卡片样式。
- 调整 `Sidebar / Header / MainLayout`,统一导航选中态、顶部栏、内容区间距和页面底色。
- 统一 Element Plus 常用控件:菜单、按钮、表格、表单、弹窗、下拉、标签、分页等基础视觉。
- 接收并补齐客户税务档案链路:企业主体抽屉增加税务档案面板,后端新增 `crm_tax_profile` 表、controller/service/mapper,前端 `taxProfileApi` 兼容 `R{data}` 与裸数据返回。
- 保留已有业务模块结构,没有删除页面或改动后端核心流程。

### 验证情况

- 已按交接要求先运行 `git log --oneline -5` 查看最近提交。
- 前端构建:已运行 `npm run build`,通过。
- 后端编译:已运行 `.codex-work/tools/apache-maven-3.9.9/bin/mvn -f zhehang-erp-server/pom.xml -pl zhehang-erp-modules -am -DskipTests compile`,通过。
- 浏览器回归:已验证 `/dashboard/home`、`/dashboard/cockpit`、`/customer/collision-manage`、`/leads/workbench`、`/order/bill`、`/finance/journal`、`/supply/channel-partner`、`/leads/enterprise-master`。
- 浏览器结果:未跳回登录页、无白屏、无 Vite 错误叠层、关键路由控制台无 error;首页截图目检通过。

### 遗留问题

- `src/styles/index.scss` 仍使用 Sass `@import`,构建有弃用警告,不阻断运行;后续可统一迁移到 `@use`。
- 本次是全局风格统一,少数复杂业务页的细节布局仍可在后续逐页精修。
- 客户税务档案已通过编译,但运行态仍需结合数据库迁移和真实后端接口做全链路联调。

### 下一步建议

- 优先逐页精修三类高频页面:营销工作台、订单提单/财务核对、财务日记账。
- 继续把工商主体、税务档案、报税提醒、客户续费做成一条完整勾稽链。
- 后续新页面默认套用 `variables.scss + feishu.scss` 的浅色办公规范,避免再出现局部深色/渐变孤岛。

---

## 2026-06-04 Codex 接手 Code 完成后的验收与小修

### 本次任务

Code 完成局部开发后,Codex 接手做构建验证、浏览器关键路由回归,并修复回归中发现的非阻断体验问题。

### 修改文件

- `zhehang-erp-ui/src/api/request.ts`
- `zhehang-erp-ui/src/api/crm.ts`
- `开发交接记录.md`

### 完成内容

- 检查工作区状态:当前前端、后端、DB 都存在大量改动,Code 未追加交接记录。
- 运行前端生产构建,确认构建通过。
- 浏览器回归 22 个关键页面:
  首页、驾驶舱、营销获客、客户中心、订单合同、服务交付、渠道管理、财务结算、业务管理、系统角色。
- 确认关键页面无白屏、未被登录页拦截、无控制台 error。
- 发现 `个人私海`、`撞单管理` 在后端接口不可用时页面已有兜底数据,但全局 axios 拦截器仍弹出“系统异常，请稍后再试”。
- 增加 axios 请求配置 `silentError`,让可降级/Mock 兜底接口不由全局拦截器弹错。
- 为 `leadApi.myList / returnToPool / distribute`、`collisionApi.getCollisionLog / resolveConflict`、`holdingApi.getCurrentHolding` 添加静默错误配置。
- 复验 `个人私海`、`撞单管理`:页面正常渲染,不再弹系统异常。

### 验证情况

- 前端构建:已运行 `npm run build`,通过。
- 浏览器验证:已打开并验证 22 个关键路由,通过。
- 控制台错误:关键路由回归未发现业务页面 console error。
- 后端编译:已通过临时 Maven 运行 `mvn -DskipTests compile`,5 个 reactor 模块全部 SUCCESS。
- Maven 说明:本机没有系统 `mvn` 和项目 Maven Wrapper,本次临时 Maven 放在 `.codex-work/tools/apache-maven-3.9.9`,该目录为本地工具缓存,不纳入提交。

### 遗留问题

- 后端已完成编译验证,但尚未启动 MySQL/Redis 做运行态接口联调。
- 工作区仍然非常脏,需要后续按模块分批 review 和建立 git 基线。
- 多个页面仍处于“前端兜底/Mock + 后端接口逐步接线”状态,不能直接当作已完全真实联调。

### 下一步建议

- 后续建议正式补 Maven Wrapper,避免每台机器重复下载 Maven。
- 让 Code 后续每次完成任务都必须追加本文件。
- 下一批建议优先推进:企业主体详情页、网销 ROI 真实归因、日记账科目自动归集、同行渠道账期应收。

---

## 2026-06-04 Codex 并行开发只读盘点

### 本次任务

Code 正在执行局部开发任务期间,Codex 先做不冲突的只读盘点,判断现有模块边界、重叠风险和后续可执行任务。

### 修改文件

- `模块地图.md`
- `并行开发盘点报告.md`
- `开发交接记录.md`

### 完成内容

- 扫描前端路由、页面、API Mock 热点和后端 modules 目录。
- 确认已有旧路径重定向方向基本正确:CRM 合同归订单合同、销售订单归提单系统、财务凭证归日记账、组织管理归 HRM。
- 修正模块地图中客户中心的主路径口径:`个人私海`、`撞单管理` 统一写为 `/customer/...`。
- 明确 `销售业绩 / 提成结算 / 财务绩效` 三者分工,避免重复开发。
- 新增 `并行开发盘点报告.md`,给 Code 完成后的验收和下一批实现任务使用。

### 验证情况

- 本次只新增/修改 Markdown 文档,未改动业务代码。
- 未运行 `npm run build`,因为没有改动前端源码。

### 遗留问题

- 工作区仍有大量未提交改动,需要等 Code 当前任务结束后统一 review。
- 多个模块仍有 localStorage Mock,需要按接口契约逐步替换,不能一次性硬删。

### 下一步建议

- 等 Code 任务结束后,先让其补写本文件。
- Codex 再执行一次构建与浏览器回归。
- 回归通过后,优先推进企业主体库详情、网销 ROI、日记账科目归集、同行渠道账期应收。

---

## 2026-06-04 Codex 建立协作基线文档

### 本次任务

在 Code 正在执行局部开发任务期间,由 Codex 建立协作基线,避免双方后续重复改模块或互相覆盖。

### 修改文件

- `开发计划.md`
- `模块地图.md`
- `数据勾稽关系.md`
- `开发交接记录.md`

### 完成内容

- 明确系统主定位: 财务公司内部经营管理系统。
- 固定主业务链路:
  `entityId -> leadId -> customerId -> contractId -> orderId -> taskId -> receiptId -> journalId`
- 建立 P0-P5 开发路线。
- 建立一级模块地图和主入口约束。
- 建立数据勾稽关系、状态流转、查重规则和跨模块校验清单。
- 建立交接记录模板和协同规则。

### 验证情况

- 本次只新增 Markdown 协作文档,未改动业务代码。
- 未运行 `npm run build`,因为文档改动不影响构建。

### 遗留问题

- 当前工作区已有大量未提交改动和未跟踪文件,需要 Code 当前任务完成后统一 review。
- 后端完整 Maven 编译此前受本机 Maven 缺失影响,仍需环境补齐后验证。

### 下一步建议

- 等 Code 当前任务结束后,先让其写入本文件。
- Codex 再做一次 review: 路由、模块重复、勾稽链路、构建结果、页面报错。
- 之后挑一条主链路继续推进: 企业主体库 -> 网销/电销线索 -> 客户 -> 订单 -> 任务 -> 回款。

---

## 交接记录模板

### 本次任务

一句话说明本次要解决什么。

### 修改文件

- `path/to/file`
- `path/to/file`

### 完成内容

- 完成点 1
- 完成点 2
- 完成点 3

### 验证情况

- 是否运行 `npm run build`:
- 是否运行后端编译/测试:
- 是否浏览器验证:
- 是否有控制台错误:

### 遗留问题

- 问题 1
- 问题 2

### 下一步建议

- 建议 1
- 建议 2
