# 浙杭集团 ERP/CRM — 继续开发方案

> 评估日期:2026-06-01 · 基于代码实测(498 个后端 Java 文件 / 126 个前端 Vue 页面 / 0 个测试)

## 一、现状判断

项目处于「**全模块骨架完成 + 前端 UI 基本成型 + CRM/呼叫中心深度迭代中**」阶段。
主体不是空壳(service 有真实实现、86/126 页面已接真实 API),但距离可上线,卡在四类问题上:

| 问题 | 现状 | 严重度 |
|---|---|---|
| 取当前用户恒为 null(类型不匹配) | createBy/updateBy 写不进、通知取不到用户 | 🔴 阻断 |
| 多租户未隔离(无 TenantLine 拦截器) | 号称多租户,实际数据不隔离 | 🔴 阻断 |
| 0 个单元/集成测试 | 498 个 Java 文件无任何测试 | 🟠 高 |
| 115 个未提交改动 + 整模块未入库 | channel/contract/order/receipt/task 仍 untracked | 🟠 高 |
| 14 个前端页面仍用内嵌 Mock 数据 | 全局还有 Mock 登录回退 | 🟡 中 |
| 后端 TODO(OAuth/Excel/聚合 controller) | 三方登录是假调用、导出未实现 | 🟡 中 |

> 本机当前缺 Java 17 / Maven / Docker,后端与 MySQL/Redis/ES 无法运行,需先搭环境。

---

## 二、分期路线

### P0 — 工程地基与硬伤修复(1~2 天)

目标:让后端能在本机真实跑起来,并修掉两个数据正确性硬伤。

1. **搭建本地环境**
   - Homebrew 安装 `openjdk@17`、`maven`、`docker`(或 OrbStack)
   - `docker-compose up -d mysql redis elasticsearch` 起依赖
   - 按 `zhehang-erp-db/init/` 编号顺序导入建库脚本,再跑 `migration/V2、V3`
   - `mvn -pl zhehang-erp-admin -am spring-boot:run` 验证启动

2. **修复「当前用户恒为 null」** (`SecurityUtils.getCurrentUserId`)
   - 现 principal 是 `LoginUser` 对象,判断却是 `instanceof Long`。
   - 改为从 `LoginUser` 取 `getUserId()`;同步修正 `getCurrentUsername` / `tenantId` 获取。
   - 影响面广(全局自动填充依赖它),修完需回归验证 createBy 落库。

3. **决策并落地多租户**
   - 若确需多租户:加 `TenantLineInnerInterceptor`,在 `insertFill` 补 `tenantId`,登录态注入当前租户。
   - 若暂不做多租户:从 `BaseEntity` 移除 `tenantId` 或文档明确「预留未启用」,避免误解。

4. **提交现有改动**
   - 整理 115 个未提交文件,把 untracked 的 channel/contract/order/receipt/task/crm 公海池模块分批 commit,建立可回溯基线。

### P1 — 前后端联调,去 Mock(3~5 天)

目标:关闭演示态,跑通真实数据链路。

1. **关闭前端 Mock 登录回退**(`views/login/index.vue` + `router/guard.ts`)
   - 移除 `mock_admin_token` 分支与 `current_role` localStorage 驱动,改为真实 `/auth/login` + 角色权限拉取。
   - 保留一个受 `import.meta.env.DEV` 控制的开关,便于无后端时调 UI。

2. **逐页替换 14 个 Mock 页面**(按业务价值排序)
   - 优先:`crm/lead`、`leads/*`(公海/撞单/个人池/分配)— 这是当前核心业务。
   - 次之:`call-center/*`(外呼/通话记录/监控)、`order/finance-check`、`hrm/attendance`。
   - 其余:`multidim/*`、`ai-chat`、`system/distribute-config`。
   - 每页:删内嵌 mock 数组 → 调 `@/api/*` → 对齐后端字段 → 校验分页/筛选。

3. **补齐前端缺口对应的后端接口**(联调中发现即补)

### P2 — 后端 TODO 与真接口接入(3~4 天)

1. **三方登录真实化**:`FeishuProvider`/`DingtalkProvider`/`WechatWorkProvider` 的 `// 替换为真实API调用`,接入真实 OAuth(需各平台 appId/secret)。
2. **Excel 导入导出**:`SysUserController`/`SysLogController` 用 EasyExcel 实现(项目已有 hutool,可选 hutool-poi)。
3. **聚合占位 controller**:`CrmController`/`OrgController`/`SalesController`/`SupplyController`/`HrmController`/`WorkflowController` 仅有 `// TODO: pending implementation` —— 确认是废弃入口则删除,否则补实现。
4. **通知模块**:`SysNotificationController` 取当前用户 ID(P0 修完后即可用 `SecurityUtils`)。

### P3 — 质量与上线准备(持续)

1. **测试**:从 0 起步,先给核心链路写集成测试 —— 登录鉴权、CRM 公海分配/撞单/回收、订单财务核对。目标先覆盖 P0/P1 改动面。
2. **安全收尾**:
   - `application-dev.yml` 里的弱口令(DB `123456`)、占位 JWT secret 走环境变量,生产用 `application-prod.yml` 覆盖。
   - 复核 `SecurityConfig` 的 permitAll 白名单与 OpenAPI 签名过滤器。
3. **CI/构建**:加一条 `mvn package` + `npm run build` + `npm run lint` 的流水线;前端 build 已配 terser 去 console。
4. **部署验证**:用 `docker-compose up --build` 全栈跑通一次。

---

## 三、风险与依赖

- **P0 第 2 项(取当前用户)影响面最大**:全局自动填充、审计日志、数据归属都依赖它,改完务必全链路回归。
- **多租户是架构级决策**:越往后改成本越高,建议在 P0 就拍板。
- **0 测试 + 115 未提交改动**:当前任何重构都缺安全网,建议「先入库基线 → 再补关键测试 → 再大改」。
- **三方登录、AI 模块**依赖外部密钥(`OPENAI_API_KEY`/`QWEN_API_KEY`/各 OAuth secret),需提前申请。

---

## 四、建议的第一步

1. 我帮你装好 Java 17 + Maven + Docker,把后端和 MySQL/Redis/ES 跑起来(P0-1)。
2. 修掉「取当前用户恒为 null」并验证落库(P0-2)。
3. 给「多租户做不做」一个明确结论。

确认后我就从第 1 步开始执行。

---

## 五、雅达参考样本吸收方向(2026-06-03)

参考包共 169 张页面截图,覆盖首页、营销、公海、合同、工商/代账/税控工单、售后、支出请款、财务、业绩、统计、人员、设置等 17 个模块。可借鉴的不是视觉皮肤,而是它的运营后台组织方式:

1. **导航更贴近业务域**
   - 左侧模块内直接展开二级菜单,用户能在一个区域完成模块定位。
   - 本系统已先落地:取消全局横向二级导航,改为左侧可展开二级菜单。

2. **列表页要服务流转**
   - 线索、合同、请款、工单页面都采用「主操作按钮 + 业务筛选 + 状态分组 + 表格 + 底部汇总」。
   - 后续用户/角色/日志以外的业务页,统一补齐批量操作、状态快捷筛选、金额/数量汇总。

3. **设置页按业务配置分层**
   - 雅达把营销配置、合同配置、工单配置、财务配置拆成二级配置中心。
   - 本系统后续应把分配规则、回收规则、流程引擎、通知公告等收束成「系统配置/业务配置」两类,减少散落页面。

4. **统计页按经营问题组织**
   - 不是单纯图表堆叠,而是营销、合同、派单、代账、财务、孵化器等专题分析。
   - 本系统驾驶舱后续应按「老板看经营、主管看过程、财务看回款」拆视角。

5. **客户服务链路更完整**
   - 雅达把营销、公海、合同、售后、客户物品、代账/工商/税控工单串起来。
   - 本系统下一批优先完善 CRM → 合同 → 工单 → 回款/请款 的闭环,比单独美化页面更有价值。

---

## 六、本轮推进记录:提成结算页(2026-06-03)

已按雅达「状态分组 + 条件筛选 + 表格汇总 + 流程闭环」的方向完善 `任务中心 / 提成结算`:

1. **页面从空壳变成可演示流程**
   - 空数据时自动补齐 8 条提成结算样例,覆盖待销售确认、待主管审核、待财务确认、待老板审批、待发放、已发放、已驳回。
   - 月度批量汇总器不再生成 0 条,会按月份生成 4 条模拟已完成订单提成。

2. **修复关键交互问题**
   - 移除页面内「当前模拟角色」切换,改为读取真实登录角色。
   - `标记已发放` 改为调用 API 并写回 localStorage,刷新后不丢状态。
   - 批量生成确认框取消后不再继续执行。
   - 销售异议/驳回动作改为持久化到结算单状态。

3. **补齐经营视图**
   - 指标条改为按全部筛选结果统计,不是只统计当前分页。
   - 增加结算单/订单/客户/销售关键词搜索、月份筛选、待发放状态分组。
   - 表格增加客户、订单、业绩金额、提成比例等字段,底部增加业绩/应发/已发/待发汇总。
   - 报表区补齐近 6 个月趋势和销售排行。

4. **统一 UI 风格**
   - 页面从深色金色主题调整为浅色运营后台风格,控件半径、卡片、表格、筛选栏与系统整体更接近。
   - 已通过 `npm run build` 和浏览器冒烟验证:页面 8 条数据正常渲染,无控制台错误。

---

## 七、本轮推进记录:模块去重与入口统一(2026-06-03)

已按「同类业务只保留一个主入口」原则做第一轮收敛:

1. **通知入口统一**
   - 保留 `系统管理 / 通知公告` 作为唯一通知入口,因为该页接真实通知 API。
   - 移除 `协作中心 / 通知中心` 的侧边栏入口,原 `/collaboration/notification` 隐藏重定向到 `/system/notification`。

2. **历史旧路径统一到当前主入口**
   - `/workflow/todo` → `/task-center/once`
   - `/crm/customer`、`/crm/follow` → `/leads/workbench`
   - `/crm/contract` → `/order/contract`
   - `/sales/order` → `/order/bill`
   - `/finance/voucher` → `/finance/journal`
   - `/finance/report` → `/dashboard/cockpit`

3. **首页/消息中心入口同步**
   - 首页快捷入口不再跳旧 CRM/Sales/Workflow/Finance 路径,改为跳当前侧边栏保留模块。
   - 消息中心待办里的审批、合同提醒跳转同步到 `任务中心` 和 `订单合同`。
   - 后端驾驶舱 mock 待办链接同步为当前主入口,避免真实接口返回旧路径。

4. **后端菜单迁移**
   - 新增 `zhehang-erp-db/migration/V5_navigation_dedupe.sql`,用于后续启用后端动态菜单时隐藏旧 CRM/Sales/Workflow/Project/Report/Message/Log 重叠入口。
   - 迁移只隐藏菜单,不删除数据和权限,后续可恢复。

验证: `npm run build` 通过;浏览器验证协作中心只剩即时消息/通讯录/视频会议/协作文档,通知只保留系统通知公告;旧路径重定向无控制台错误。

---

## 八、本轮推进记录:合同主入口完善(2026-06-03)

已按「订单合同只保留 `/order/contract` 一个主入口」继续补强合同页:

1. **补齐合同生命周期样例**
   - `contract-mgmt.ts` 新增 9 条合同样例,覆盖草稿、待签署、部分签署、已生效、履行中、即将到期、已续签、已终止。
   - 新增 `ensureSamples()`,避免 localStorage 之前已写入空数组时页面继续空白。

2. **补齐关联提单数据**
   - `order.ts` 新增已完成、待财务确认、草稿等提单样例。
   - 合同页初始化时自动确保提单样例存在,生成合同弹窗可直接选择已完成提单。

3. **清理不理想文案**
   - 移除「API 待对接 / 接口待对接」这类直露技术文案。
   - 电子签署改为业务可接受的「电子签署通道已预留」说明,后续接入电子签章后再自动回写。
   - 把「卷宗」等偏法律皮肤的旧表达调整为「合同运营台 / 合同列表 / 合同详情」。

4. **统一 UI 风格**
   - 保留原来的合同起草、签署、续签、终止、模板管理、到期阶梯处理功能。
   - 视觉从深色卷宗风调整为浅色运营后台风格,卡片、表格、按钮、抽屉更贴近当前系统。

验证: `npm run build` 通过;浏览器验证 `/order/contract` 正常显示 9 条合同样例,生成合同弹窗可展开已完成提单下拉,无控制台错误。

---

## 九、本轮推进记录:提单页与合同联动(2026-06-03)

已顺着「提单 → 财务确认 → 合同草稿」链路继续完善 `订单管理 / 提单系统`:

1. **提单样例与新建订单可用性**
   - `order.ts` 补齐 8 条提单样例,覆盖草稿、待财务确认、已完成等状态。
   - 提单页初始化时自动补样例,空库进入页面不会白屏。
   - 新建订单客户下拉补充客户候选,并修复默认显示 `0` 的问题。

2. **搜索与状态筛选修复**
   - 新增 `keyword` 查询,按订单编号、客户名称、提单人做 OR 搜索。
   - 修复原先同一个关键词被当成多个 AND 条件导致搜不到的问题。
   - 统计卡片、Tab、关键词、日期筛选统一走 `applyFilters()`,自动回到第一页并刷新列表。

3. **合同联动真实生效**
   - 订单实际审批完成时调用收款/合同联动,自动生成合同草稿。
   - 样例初始化不会批量抢先生合同,避免覆盖合同页自己的生命周期样例。
   - 页面提示从「合同将自动生成」改成「合同草稿已自动生成」。

4. **本地预览登录兜底**
   - 开发环境后端登录不可用时允许回退 Mock 登录,生产环境不受影响。
   - 解决本地预览会话丢失后被路由守卫挡回登录页的问题。

5. **统一 UI 风格**
   - 提单页从深色金色主题调整为浅色运营后台风格。
   - 清理「占位 · 演示模式」等不理想可见文案。

验证: `npm run build` 通过;浏览器验证开发登录兜底可进入 `/order/bill`,列表显示 8 条样例,`陈苗` 关键词搜索只返回相关订单,新建订单客户下拉可用,无控制台错误。

---

## 十、本轮推进记录:财务核对与合同联动(2026-06-03)

已继续补强 `订单管理 / 财务核对`,把它接入「提单 → 收款核销 → 合同草稿 → 发票/退款时间线」闭环:

1. **财务样例数据落地**
   - `receipt.ts` 补齐收款、分期计划、发票、退款、财务联动时间线样例。
   - 新增 `ensureSamples()`,解决 localStorage 曾经写入空数组后页面继续空白的问题。
   - 样例覆盖待确认收款、已确认收款、逾期应收、待开票、已开票、待审批退款、已完成退款。

2. **确认收款链路真实联动**
   - `receiptApi.confirm()` 内部完成收款确认、分期计划核销、最后一期判断、待开票生成、时间线写入。
   - 最后一期/一次性全款确认后调用合同联动,自动生成合同草稿;合同已存在或订单缺失时会把检查结果写入时间线,不再只靠页面提示。
   - 页面去掉重复调用合同联动的逻辑,避免同一笔收款被前端和 API 双写。

3. **页面文案与可用性调整**
   - 移除「占位」「合同将自动生成」等不理想可见文案。
   - 到账凭证、退款凭证改为正式业务表述。
   - 初始化时先确保提单与财务样例存在,再加载收款、计划、发票、退款、汇总、时间线。

4. **统一 UI 风格**
   - 财务核对页从深色金色主题调整为浅色运营后台风格。
   - 表格、指标条、弹窗、逾期阶梯、发票流转、退款流程、时间线统一浅色卡片和清晰边框。

验证: `npm run build` 通过;浏览器验证 `/order/finance-check` 首屏显示 2 条待核对,确认杭州启明全款后待核对变 1 条,时间线显示「收款确认 / 订单全额收齐 / 合同草稿生成 HT202606030010」,无控制台错误。
