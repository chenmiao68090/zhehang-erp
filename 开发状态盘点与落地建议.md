# 浙杭ERP 开发状态盘点与落地建议（2026-06-10）

> 背景：老板感觉"很多都没落地"。本报告用四路并行体检取证：①数据库业务数据量 ②前端是否真连后端 ③最近交付清单 ④后端接口健康（本次因执行环境故障未跑完，待补）。
> **结论先行：您的感觉是对的。后端这两周补了大量"地基"（权限/闭环/自动回收），但员工每天打开的前端页面有约四成是假的（mock/写死），后端新功能在界面上没有入口——所以"做了很多"和"用得上"之间断了。**

---

## 一、三个硬证据（为什么感觉没落地）

### 证据1：数据库里几乎没有真实业务数据
全库体检（49 张核心业务表）：
- **有数据的只有获客半条链**：线索 crm_lead ≈3 条（演示数据）、客户 crm_customer ≈7 条（多为测试）、营销活动 1 条。
- **成交/履约/收款全链 0 行**：订单、合同、收款、交付任务、销售订单/回款/提成、财务应收/发票/凭证——全部 0。
- **跟进记录 crm_follow = 0 行**：电销的核心动作"跟进"从来没人在系统里真正做过。
- **员工档案 org_employee = 0 行**：25 个登录账号没有任何 HR 档案。
- HRM/供应链/项目/工作流/文件/知识库模块全部 0 行 = 纯空壳。

**含义**：系统至今没有承载过一单真实业务。功能"建了"和"用了"是两回事，数据量是落地的唯一标尺。

### 证据2：前端约四成页面是"假的"（这是最大断点）
前端 116 个页面中：
- **9 个 api 文件是纯 localStorage 假实现，从不发请求**：`order.ts`（提单）、`contract-mgmt.ts`（合同）、`receipt.ts`（回款）、`task-center.ts`（任务中心）、`channel.ts`（渠道/供应）、`call-center.ts`（呼叫中心）、`growth.ts`、`private-domain.ts`、`notification.ts`。
  → **员工在"提单→合同→财务核对→派任务"里点的每一下，数据都存在自己浏览器里，根本没进数据库**。而后端这些真实接口（BizOrder/BizContract/BizReceipt/BizTask…）早就存在且已修好。
- **9 个页面写死中文假数据**（薪酬"¥812,400"、HR 档案等），按钮全是摆设。
- **首页驾驶舱的数据也是假的**（来自 localStorage 快照，没调真实 dashboard 接口）。
- **Mock 登录后门**：开发模式下后端一报错，前端就静默假登录成 admin——掩盖一切后端问题。
- 真正连后端的页面约 60 个（CRM 线索/客户、系统管理、组织、销售、多维表等）。

### 证据3：后端新功能在界面上没有入口
最近做的营销闭环 5 个关键接口，**前端一个都没调**：
| 功能 | 后端 | 前端 |
|---|---|---|
| 今天该打谁（todo-follow） | ✅ | ❌ 无页面调用 |
| 回收预警（recycle-warning） | ✅ | ❌ |
| 手动触发回收（recycle/run） | ✅ | ❌ 只能命令行触发 |
| 批量公司入池（import-companies） | ✅ | ❌ 前端还在调旧接口 |
| 转化率汇总（stats/conversion） | ✅ | ❌ |

另发现前后端接口对不上：前端改/删回收规则调的 `PUT/DELETE /crm/recycle/rules` 后端不存在。

---

## 二、最近真正交付了什么（后端，已推 GitHub）
1. **数据权限铺满全系统**：电销看本人、主管看本部门、只有财务部+老板看全部财务；覆盖 CRM/订单/合同/收款/提成/销售/财务/HR/供应链/项目。
2. **业务后半段自动串联**：签约→派交付任务→收款确认→合同状态回写→自动提成。
3. **救活一批 500 死接口**（表结构与代码漂移），含 V25 销售表修复。
4. **营销闭环**：自动补工商、批量入池、待跟进、自动回收（每日02:00）+回收预警、转化率/ROI 归因。
5. 两份分析文档：页面审计报告、权限数据范围矩阵。

这些是"地基"，质量是真的，但**老板和员工在界面上看不见**——这就是体感差的来源。

---

## 三、落地路线建议（按优先级）

### P0 本周必做（把"真用"变为可能）——我可以直接做
1. **前端换真接口**：把 order/contract-mgmt/receipt/task-center/channel 这 5 个 mock api 换成真实后端调用，删掉 biz-linkage 假联动。这是"提单→合同→收款→任务"核心链落地的唯一堵点。
2. **营销闭环补界面入口**：电销工作台加"今天该打谁/回收预警"卡片；公海页加"按关键词批量入池"按钮；统计页接 conversion/roi。
3. **建员工档案**：从 25 个 sys_user 生成 org_employee 并回填 user_id——否则提成/薪资/考勤"看本人"永远是空的。
4. **封掉 mock 登录后退**：dev 模式后端报错时不再假登录，让问题暴露而不是被掩盖。
5. **修 recycleApi 前后端不匹配**（PUT/DELETE 缺失）。

### P1 下周（让"真用"发生）
6. **选一条链强制真跑**：电销团队每天在系统里领取→拨打→写跟进→转化，一周后看 crm_follow 行数。**数据涨了才叫落地，页面再多不算。**
7. **9 个写死假数据页**：要么接线、要么先从菜单摘掉——员工点开假页面会失去对系统的信任。
8. **4 个悬而未决的拍板**（财务范围宽窄/待指派任务可见性/网销角色/会计拆分）给个答案，我按答案收口。

### P2 之后
9. 页面审计报告里的 UI 改造（看板化 13 页/规则中心 8 模块/菜单≤12）——目前只做了 1 条，其余无提交痕迹。
10. 132 个接口全量探活补测（本次因工具环境故障没跑完，环境恢复即补）。

---

## 四、一句话总结
**后端地基已经能用，前端还有四成是布景板；数据库里没有一单真实业务。** 把 5 个 mock api 换真、给新功能开界面入口、建好员工档案，然后逼一条链真跑一周——落地感会立刻不一样。
