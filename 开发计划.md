# 浙杭集团财税运营系统开发计划

> 版本: 2026-06-04  
> 用途: 作为 Codex 与 Code 协同开发的总路线图。任何新增页面、接口、菜单、字段都应先对齐本计划。

## 一、项目定位

本系统不是传统泛 ERP,而是面向财务公司的内部经营管理系统。核心目标是把营销获客、客户建档、合同订单、财税交付、渠道地址、回款核销、续费复购和经营分析串成一条可追溯链路。

主链路:

```text
企业主体 entityId
-> 线索 leadId
-> 客户 customerId
-> 合同 contractId
-> 订单 orderId
-> 交付任务 taskId
-> 回款 receiptId
-> 财务日记账 journalId
```

## 二、协同分工

### Codex 负责

- 系统架构、业务链路和模块边界。
- 判断功能保留、合并、删除。
- 统一菜单、术语、数据勾稽关系。
- 最终验收 Code 的局部开发结果。
- 处理跨模块问题和页面之间的数据关系。

### Code 负责

- 单页面、单组件、表单、表格、弹窗、局部样式。
- 按明确任务实现具体页面能力。
- 小范围 bug 修复。
- 每次完成后写入 `开发交接记录.md`。

### 协同原则

- 不同时修改同一个核心文件,尤其是 `routes.ts`、`growth.ts`、全局布局、权限守卫、菜单结构。
- 大改之前先写清楚任务范围。
- 每次只围绕一个业务闭环或一个页面做改动。
- 完成后必须记录: 改了什么、验证了什么、还有什么风险。

## 三、阶段路线

### P0 工程地基

目标: 系统能稳定登录、稳定预览、核心数据不乱。

任务:

- 稳定登录、验证码、Token、权限守卫。
- 明确当前用户、创建人、审核人、部门归属。
- 明确多租户 `tenantId` 是否启用。
- 清理重复菜单、旧路径、孤立页面。
- 整理未提交改动,建立可回滚基线。
- 建立协作文档和交接规范。

验收:

- 前端 `npm run build` 通过。
- 登录后刷新不白屏。
- 菜单无明显重复入口。
- 新增功能能写入交接记录。

### P1 主业务闭环

目标: 跑通财务公司主业务链路。

任务:

- 企业主体库作为全系统主数据根。
- 公司名称自动带出工商信息。
- 网销、电销、拓客情报统一进入线索池。
- 线索转客户时继承工商、税务、联系人、风险标签。
- 客户成交后生成合同、订单、服务项目。
- 合同/订单自动触发交付任务。
- 回款核销自动关联订单和日记账。

验收:

- 一个客户能从线索走到订单、任务、回款。
- 每个关键对象都能追溯上游来源。
- 页面上能看到勾稽摘要。

### P2 营销获客增强

目标: 对标探迹、销帮帮、销售型 CRM,但保留财税公司业务特色。

任务:

- 新注册公司监控: T+1、T+7。
- 工商异常监控: 地址异常、经营异常、简易注销。
- 税务异常监控: 非正常户、欠税公告、税务处罚。
- 网销 ROI: 渠道成本、线索数、成交额、回款额。
- 电销工作台: 拨打记录、意向评级、AI 摘要、下次跟进。
- 撞单管理: 公司名、信用代码、手机号、联系人多维查重。

验收:

- 高价值线索能优先分配。
- 重复线索能预警。
- ROI 能按渠道和服务类型统计。

### P3 财税交付与财务管理

目标: 让系统真正服务代理记账、工商注册、税务异常、地址挂靠和内部财务。

任务:

- 代理记账客户台账。
- 月度记账、纳税申报、工商年报周期任务。
- 工商注册、变更、注销、异常解除专项任务。
- 挂靠地址资源池和同行渠道订单。
- 日记账升级为类多维表格。
- 应收、实收、退款、欠款、坏账、提成勾稽。
- 费用报销、备用金、工资、成本归集。

验收:

- 每笔收入和支出有一级归集。
- 订单、收款、提成、日记账能互相追溯。
- 周期任务能自动生成。

### P4 经营驾驶舱

目标: 老板每天打开系统就能知道哪里赚钱、哪里漏钱、哪里有风险。

任务:

- 首页经营摘要。
- 经营驾驶舱下钻。
- 网销 ROI、渠道应收、地址资源库存、续费风险、交付逾期。
- AI 经营分析。
- 每日、每周、月度经营报告。

验收:

- 每个指标能解释口径。
- 风险能跳转到处理页面。
- 看板数据和业务模块一致。

### P5 上线质量

目标: 可以长期稳定使用。

任务:

- 前后端真实接口替换 Mock。
- MySQL 表结构和迁移脚本完善。
- Redis 登录状态、缓存、限流。
- 权限、角色、数据范围。
- 操作日志、登录日志、审计日志。
- Excel 导入导出。
- 数据备份和恢复。
- 部署脚本和环境说明。
- 关键业务测试用例。

验收:

- 可部署、可备份、可恢复。
- 权限可控,数据不乱。
- 关键流程有测试或明确验收清单。

## 四、当前优先级

当前阶段建议集中在 P0 + P1:

1. 固定模块地图和主入口。
2. 确认 `entityId` 为企业主体主键。
3. 打通企业主体库 -> 网销/电销线索 -> 客户 -> 订单 -> 任务 -> 回款。
4. 清理重复模块和旧入口。
5. 每完成一批就记录交接并准备建立 git 基线。

## 五、每次开发验收清单

每个任务完成后至少检查:

- 是否改动了约定范围外的文件。
- 是否新增了重复入口或重复页面。
- 是否保持 `entityId` / `customerId` / `orderId` 等主键勾稽。
- 是否有空状态、加载状态、错误提示。
- 是否有按钮权限或状态流转约束。
- 前端任务是否跑过 `npm run build`。
- 后端任务是否能编译或说明无法编译原因。
- 是否更新 `开发交接记录.md`。
