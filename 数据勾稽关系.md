# 数据勾稽关系

> 版本: 2026-06-04  
> 用途: 定义系统核心对象之间的主键关系、流转关系和校验规则。

## 一、总链路

```text
entityId 企业主体
  -> leadId 线索
  -> customerId 客户
  -> contractId 合同
  -> orderId 订单
  -> serviceItemId 服务项目
  -> taskId 交付任务
  -> receiptId 回款
  -> journalId 财务日记账
```

所有新功能都要尽量挂到这条链路上。无法挂接时,需要说明原因,并标记为“待核验”或“临时记录”。

## 二、核心对象

### entityId 企业主体

定位: 企业事实主数据根。

来源:

- 工商接口或本地企业主体库。
- 网销表单输入公司名称后自动匹配。
- 拓客情报命中公司后写入。
- 未命中时允许创建“待工商核验主体”。

关键字段:

| 字段 | 说明 |
|---|---|
| `entityId` | 企业主体 ID |
| `companyName` / `name` | 企业全称 |
| `creditCode` | 统一社会信用代码 |
| `taxNo` | 税号 |
| `legalPerson` | 法人 |
| `registeredCapital` | 注册资本 |
| `establishDate` | 成立日期 |
| `businessStatus` | 工商状态 |
| `taxQualification` | 税务资质 |
| `riskTags` | 工商/税务/营销标签 |

规则:

- 公司名称和统一社会信用代码是查重核心。
- 线索、客户、订单、任务、回款都应尽量能追溯 `entityId`。
- 如果公司名命中但信用代码缺失,状态应标记为“待工商核验”。

### leadId 线索

定位: 营销获客记录。

来源:

- 网销线索: 官网、企微、抖音、小红书、百度推广、老客转介绍。
- 拓客情报: 新注册公司、税务异常、经营异常、地址异常、年报客群。
- 电销导入或手工新增。

关键字段:

| 字段 | 说明 |
|---|---|
| `leadId` | 线索 ID |
| `entityId` | 绑定企业主体 |
| `source` / `channel` | 来源渠道 |
| `demand` | 客户需求 |
| `score` | 线索评分 |
| `duplicateRisk` | 查重风险 |
| `status` | 新线索、有效、已转电销、已提单、流失 |

规则:

- 新线索入库前先按公司名/信用代码/手机号查重。
- 重复风险为 `hit` 时,不应直接生成新客户,先进入撞单处理。
- 有效线索才能进入电销或直接提单。

### customerId 客户

定位: 和公司发生业务关系后的客户主数据。

来源:

- 线索转客户。
- 企业主体库直接生成客户。
- 老客户导入。

关键字段:

| 字段 | 说明 |
|---|---|
| `customerId` | 客户 ID |
| `entityId` | 企业主体 |
| `customerName` | 客户名称 |
| `ownerId` | 负责人 |
| `poolType` | 公海、私海、线上、电销等 |
| `followStage` | 跟进阶段 |
| `dealStatus` | 成交状态 |

规则:

- 同一个 `entityId` 原则上只保留一个活跃客户主档。
- 允许多个线索归到同一客户。
- 客户详情必须展示线索、合同、订单、任务、回款摘要。

### contractId 合同

定位: 约定服务内容、价格和周期的法律/业务凭证。

来源:

- 提单完成后自动生成合同草稿。
- 手工起草合同。
- 续费生成合同。

关键字段:

| 字段 | 说明 |
|---|---|
| `contractId` | 合同 ID |
| `customerId` | 客户 |
| `orderId` | 来源订单 |
| `contractNo` | 合同编号 |
| `status` | 草稿、待签署、生效、履行中、到期、终止 |
| `amount` | 合同金额 |
| `signDate` | 签署日期 |
| `expireDate` | 到期日期 |

规则:

- 合同应优先从订单生成。
- 合同金额要和订单应收计划保持一致。
- 代理记账、地址挂靠等周期服务必须有到期提醒。

### orderId 订单

定位: 成交后的业务执行和收款对象。

来源:

- 线索提单。
- 合同生成订单。
- 手工新增订单。

关键字段:

| 字段 | 说明 |
|---|---|
| `orderId` | 订单 ID |
| `customerId` | 客户 |
| `contractId` | 合同 |
| `sourceLeadId` | 来源线索 |
| `sourceChannel` | 来源渠道 |
| `finalAmount` | 成交金额 |
| `receiptStatus` | 回款状态 |
| `deliveryStatus` | 交付状态 |

规则:

- 订单创建后必须生成服务项目。
- 订单审批通过后才进入财务核对和交付。
- 订单金额要和回款、提成、日记账勾稽。

### taskId 交付任务

定位: 服务交付执行单元。

来源:

- 订单服务项目自动拆解。
- 周期服务自动生成。
- 客户满意度或异常反馈升级生成。

常见任务:

- 客户资料收集。
- 建账初始化。
- 月度记账。
- 纳税申报。
- 工商注册。
- 工商变更。
- 税务异常解除。
- 地址异常解除。
- 年度工商年报。

规则:

- 任务必须绑定 `customerId`。
- 能绑定 `orderId` 的必须绑定订单。
- 周期任务按服务周期自动生成。
- 逾期任务要反向预警到首页和驾驶舱。

### receiptId 回款

定位: 订单实收款和应收核销。

来源:

- 财务核对。
- 银行流水导入。
- 手工登记。

关键字段:

| 字段 | 说明 |
|---|---|
| `receiptId` | 回款 ID |
| `customerId` | 客户 |
| `orderId` | 订单 |
| `amount` | 回款金额 |
| `status` | 待确认、已确认、退回、作废 |
| `receiptDate` | 回款日期 |

规则:

- 回款确认后更新订单回款状态。
- 回款确认后应生成或提示生成日记账。
- 同行渠道回款必须关联渠道账期和应收余额。

### journalId 财务日记账

定位: 最终收支流水和多维财务归集。

来源:

- 回款确认自动生成。
- 支出/报销/成本手动或自动生成。
- 银行流水导入。

关键字段:

| 字段 | 说明 |
|---|---|
| `journalId` | 日记账 ID |
| `direction` | 收入、支出、调拨、冲减 |
| `amount` | 金额 |
| `primaryCategory` | 一级归集 |
| `category` | 二级科目 |
| `customerId` | 客户 |
| `orderId` | 订单 |
| `receiptId` | 回款 |
| `project` | 项目/业务线 |

规则:

- 收入类流水优先绑定回款和订单。
- 支出类流水必须归集到一级模块。
- 不允许大量“其他”科目堆积。

## 三、状态流转

### 线索状态

```text
new 新线索
-> qualified 有效线索
-> queued 已转电销
-> opportunity 意向商机
-> ordered 已提单
-> lost 流失
```

### 订单状态

```text
draft 草稿
-> pending_review 待审核
-> finance_confirming 财务确认
-> approved 已通过
-> contracted 已生成合同
-> delivering 交付中
-> completed 已完成
-> cancelled 已取消
```

### 任务状态

```text
assigned 已分配
-> processing 处理中
-> reviewing 待验收
-> completed 已完成
-> overdue 逾期
-> rejected 驳回
```

### 回款状态

```text
pending 待确认
-> confirmed 已确认
-> refunded 已退款
-> void 作废
```

## 四、查重规则

强命中:

- 统一社会信用代码一致。
- `entityId` 一致。
- 客户主档已存在且公司名称标准化后一致。

疑似命中:

- 公司简称相似。
- 手机号一致。
- 法人或联系人一致。
- 同一企业已有线索但未生成客户。

无重复:

- 公司名、信用代码、手机号均未命中。

处理规则:

- 强命中: 进入撞单处理或归并到已有客户。
- 疑似命中: 提醒销售确认。
- 无重复: 可正常入库。

## 五、跨模块校验清单

新增功能前确认:

- 是否需要 `entityId`。
- 是否会生成新客户。
- 是否会影响订单、任务、回款。
- 是否需要查重。
- 是否需要权限控制。
- 是否需要状态流转。

开发完成后确认:

- 从当前页面能否跳转上游/下游。
- 列表、详情、弹窗里的 ID 和名称是否一致。
- 金额是否能追溯到订单和回款。
- 任务是否能追溯到客户和订单。
- 日记账是否能追溯到回款或支出来源。
