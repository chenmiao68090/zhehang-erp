# 浙杭ERP 数据范围（谁能看到谁的数据）矩阵

> 配合「权限/归属/数据范围优先稳定」。本表是当前后端**已实现**的行为（2026-06-09）。
> 机制：登录时算好每人的 `dataScope`（角色 data_scope 取最小值=范围最大）+ `roleKeys`；查询列表时按归属人(owner)/部门(dept)过滤。

## 一、角色与数据范围档位（来自 sys_role.data_scope）
| 角色 role_key | data_scope | 含义 | 谁 |
|---|---|---|---|
| super_admin / sys_admin | 1 | 全部 | 陈苗(老板)、admin |
| **finance_hq（财务部）** | 1 | 全部(财务对账) | 财务部(dept110)成员，如李向荣 |
| dept_manager | 4 | 本部门及以下 | 张华瑶/彭珍珍/姜奕承(各部门主管) |
| finance | 5 | 仅本人 | 做账会计、地址管家、财务管家 |
| sales / online_sales(网销) | 5 | 仅本人 | 12 电销 / 网销(待分配) |
| staff | 5 | 仅本人 | 刻章/工商渠道/人事等 |
> 重要(老板2026-06-09定)：做账会计、管家只看自己；**只有「财务部(finance_hq)」成员 + 老板**看全部财务。会计若被加 finance_hq 才会看全部。

## 二、各角色在各模块能看到什么（已实现）
| 模块/表 | 电销 sales | 部门主管 dept_manager | 财务 finance | 老板/admin |
|---|---|---|---|---|
| 线索 crm_lead | 本人(owner) | 本部门及以下 | 本人 | 全部 |
| 客户 crm_customer | 本人 | 本部门及以下 | 本人 | 全部 |
| **订单 biz_order** | 本人(salesman) | 本部门及以下 | **全部(对账)** | 全部 |
| **合同 biz_contract** | 本人 | 本部门及以下 | **全部** | 全部 |
| **收款 biz_receipt** | 本人订单的收款 | 本部门 | **全部(对账)** | 全部 |
| **提成 biz_commission** | 本人 | 本部门 | **全部(核发)** | 全部 |
| **交付任务 biz_task** | 分配给自己的 ＋ 待指派 | 全部(派活) | 分配给自己的＋待指派 | 全部 |
| 公海线索/客户池 | 共享(可领取) | 共享 | 共享 | 共享 |

> 说明（2026-06-09 按老板反馈已更正，上表"财务 finance"列以本说明为准）：
> - 财务类表（订单/合同/收款/提成）**只有「财务部(finance_hq, data_scope=1)」成员 + 老板/admin 看全部**（对账/确认/核发）。
>   做账会计、地址/财务管家虽是 finance 角色，但 data_scope=5 → **只看本人**（会计无订单即看到空）。
> - 交付任务 biz_task 已补 dept_id：**主管只看本部门(及以下)交付任务 + 待指派**；执行人看「分配给自己的 + 待指派」；老板/财务部看全部。
> - 公海是设计上共享的（谁都能领），不做数据范围过滤。
> - 归属字段：线索/客户=owner_id+dept_id；订单/提成=salesman_id+dept_id；合同/收款=从订单继承 salesman_id+dept_id。

## 三、⚠️ 需老板拍板（影响上表）
1. **财务"看全部财务数据"是否过宽**：当前所有 finance 角色（含地址管家/财务管家）都能看全公司订单/合同/收款/提成。
   - 选项A（现状）：财务都看全部，简单，适合小团队。
   - 选项B：拆 `finance` 为「会计(看全部对账)」+「财税/管家(看本人)」两个角色，更精细。
2. **网销** 目前没独立角色，随 sales（看本人）。若网销要独立可见范围，需定角色或按 teamType 过滤。
3. **部门主管看交付任务**：当前主管看全部任务（任务表无部门字段）。若要"主管只看本部门交付"，需给 biz_task 补 dept_id。
4. **deptId 为空的人**：若某主管没配部门，会退化为"只看本人"（安全但偏紧）。当前 25 人都已配部门，无此问题；新增用户务必配 dept_id。

## 四、自检 SQL（随时核对各人范围，只读）
```sql
SELECT u.id,u.username,u.nickname,u.dept_id,d.dept_name,r.role_key,r.data_scope
FROM sys_user u LEFT JOIN sys_dept d ON u.dept_id=d.id
LEFT JOIN sys_user_role ur ON ur.user_id=u.id LEFT JOIN sys_role r ON r.id=ur.role_id
ORDER BY u.dept_id,u.id;
```
